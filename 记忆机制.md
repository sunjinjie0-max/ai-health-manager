# AI 健康助手 Agent 记忆机制

## 1. 整体设计

当前项目的记忆机制不是简单把历史消息全部塞进 Prompt，而是由以下几部分组成：

1. 短期会话记忆
2. PostgreSQL 用户画像与健康档案
3. Elasticsearch 长期语义记忆
4. Memory Routing 记忆路由
5. 回答生成阶段的记忆注入与直接记忆回答

在 `HealthAdvisorAgent` 的 LangGraph 流程中，记忆相关节点位于主流程中间：

```text
load_context
→ check_safety
→ classify_intent
→ memory_route
→ retrieve_memory
→ plan_tasks / rag_retrieve / generate_response
→ post_process
```

相关代码：

- `ai-health-manager-backend/app/agents/health_advisor/agent.py`
- `ai-health-manager-backend/app/agents/health_advisor/nodes/load_context.py`
- `ai-health-manager-backend/app/agents/health_advisor/nodes/memory_route.py`
- `ai-health-manager-backend/app/agents/health_advisor/nodes/retrieve_memory.py`
- `ai-health-manager-backend/app/agents/health_advisor/nodes/generate_response.py`
- `ai-health-manager-backend/app/agents/health_advisor/nodes/post_process.py`
- `ai-health-manager-backend/app/memory/long_term.py`
- `ai-health-manager-backend/app/memory/short_term.py`
- `ai-health-manager-backend/app/memory/profile.py`

## 2. 短期记忆

短期记忆用于保存当前会话最近几轮对话，主要解决同一 session 内的追问问题，例如：

```text
我习惯周末运动。
我一般什么时候运动？
```

实现位置：

```text
ai-health-manager-backend/app/memory/short_term.py
```

当前短期记忆是进程内内存实现：

```text
ShortTermMemory(max_turns=10)
```

它按 `session_id` 保存最近消息，最多保留约 20 条 user / assistant 消息。

写入发生在 `post_process` 阶段：

```text
用户消息 + 助手回答 → short_term_memory.add_message(...)
```

读取发生在 `load_context` 阶段：

```text
short_term_memory.get_history(session_id)
```

如果 `memory_route` 判断当前问题需要短期记忆，`retrieve_memory` 会选择最近的短期消息：

```text
selected_short = short_history[-8:]
```

当前边界：

1. 短期记忆不是 Redis，而是进程内内存。
2. 服务重启会丢失短期记忆。
3. 多实例部署时，不同实例之间不会共享短期记忆。

## 3. 用户画像与健康档案

用户画像是结构化数据，不属于向量记忆。

`load_context` 会从 PostgreSQL 中加载：

1. `basic_info`
2. `health_status`
3. `lifestyle`
4. `health_goals`
5. `diet_preferences`

同时还会加载最近 30 天健康数据摘要，例如：

1. 步数
2. 睡眠
3. 心率
4. 健康数据统计与 insight

相关代码：

```text
ai-health-manager-backend/app/memory/profile.py
ai-health-manager-backend/app/agents/health_advisor/nodes/load_context.py
```

这部分主要用于个性化建议，例如：

```text
结合用户年龄、健康目标、近期睡眠和运动记录生成建议。
```

## 4. 长期语义记忆

长期记忆用于保存跨会话、稳定、可复用的用户事实和偏好。

实现位置：

```text
ai-health-manager-backend/app/memory/long_term.py
```

存储后端是 Elasticsearch，默认索引名：

```text
ai_health_long_term_memory
```

长期记忆不是保存完整聊天记录，而是保存从用户消息中抽取出的稳定事实，例如：

```text
用户习惯周六运动。
用户常进行的运动项目是跑步。
用户饮食约束包含低盐。
用户经常熬夜。
用户当前健康目标包含减脂。
用户对花生过敏。
```

每条长期记忆包含：

```text
user_id
session_id
domain
content
source_message
assistant_response
memory_type
topic
slot
value
confidence
evidence
tags
salience
embedding
created_at
updated_at
```

配置项位于：

```text
ai-health-manager-backend/app/config.py
```

关键配置：

```text
memory_index_name = ai_health_long_term_memory
memory_top_k = 6
memory_candidate_k = 16
memory_vector_weight = 0.7
memory_bm25_weight = 0.3
memory_max_days = 365
memory_write_enabled = true
memory_use_llm_supplement = true
```

## 5. 长期记忆抽取

长期记忆写入发生在 `post_process` 阶段：

```text
long_term_memory.store_user_message_memories(...)
```

抽取方式是：

```text
规则抽取优先 + 必要时 LLM 补充
```

### 5.1 规则抽取

规则抽取覆盖多个领域。

运动领域：

1. 运动时间
2. 运动项目
3. 运动频率

示例：

```text
我习惯周六运动，经常跑步。
→ 用户习惯周六运动。
→ 用户常进行的运动项目是跑步。
```

饮食领域：

1. 忌口
2. 偏好
3. 低盐
4. 控糖
5. 低脂
6. 素食
7. 夜宵
8. 不吃早餐

睡眠领域：

1. 熬夜
2. 午睡
3. 周末补觉
4. 睡眠模式

生活方式领域：

1. 经常加班
2. 久坐较多
3. 通勤时间较长
4. 工作日可用于运动的时间较少

健康目标与健康状况：

1. 减脂
2. 增肌
3. 控糖
4. 控压
5. 高血压
6. 糖尿病
7. 高血脂
8. 失眠

安全与饮食禁忌：

1. 食物过敏
2. 明确不吃某类食物

### 5.2 LLM 补充抽取

LLM 补充不是每次都调用。

触发条件在 `LongTermMemory._should_call_llm_supplement()` 中，主要包括：

1. `memory_use_llm_supplement = true`
2. 用户消息长度足够
3. 消息中有第一人称或用户自身相关表达
4. 命中健康领域关键词
5. 规则抽取结果较少
6. 消息涉及多个领域
7. 表达比较隐含

典型场景：

```text
最近一忙就顾不上吃早餐，晚上还经常加班到很晚，运动也只能放到周末。
```

规则可能能抽到一部分，但 LLM 可以补充更完整的长期事实，例如：

```text
用户经常不吃早餐。
用户经常加班。
用户工作日可用于运动的时间较少。
用户倾向周末运动。
```

LLM 补充抽取有过滤规则：

1. 只保存稳定、跨会话可复用的信息。
2. 不保存一次性的短期状态。
3. 不保存寒暄、模糊情绪。
4. 置信度低于阈值的结果会被丢弃。
5. 与规则抽取重复的内容会去重。

## 6. 长期记忆存储

长期记忆写入 ES 时会先生成 embedding：

```text
EmbeddingModel.encode(content)
```

然后写入 Elasticsearch dense vector 字段：

```text
embedding: dense_vector
```

索引 mapping 包括：

```text
user_id: keyword
session_id: keyword
domain: keyword
content: text
source_message: text
memory_type: keyword
topic: keyword
slot: keyword
value: keyword
confidence: float
evidence: text
tags: keyword
salience: float
embedding: dense_vector
```

长期记忆 ID 是稳定 ID：

```text
stable_memory_id(user_id, memory_type, topic, content)
```

这样可以减少重复写入。

## 7. 长期记忆检索

当 `memory_route` 判断需要长期记忆时，`retrieve_memory` 会调用：

```text
long_term_memory.search_relevant_messages(...)
```

优先走 Elasticsearch 语义检索：

```text
BM25 关键词检索
+ Dense Vector 向量检索
+ RRF 融合
+ 去噪过滤
```

检索特性：

1. 按 `user_id` 隔离。
2. 支持排除当前 session。
3. 默认检索最近 180 天。
4. 默认返回 top 8。
5. 支持 BM25 与向量权重配置。
6. ES 不可用时回退到数据库历史消息关键词检索。

如果 ES 检索成功，会返回类似：

```json
{
  "role": "memory",
  "content": "用户习惯周六运动。",
  "memory_type": "habit",
  "topic": "exercise_time",
  "metadata": {
    "domain": "exercise",
    "slot": "exercise_time",
    "value": "周六",
    "confidence": 0.9
  }
}
```

## 8. Memory Routing

Memory Routing 的作用是判断当前问题需要哪类上下文：

1. 只需要短期记忆
2. 只需要长期记忆
3. 只需要 RAG
4. 需要短期记忆 + RAG
5. 需要长期记忆 + RAG

实现位置：

```text
ai-health-manager-backend/app/agents/health_advisor/nodes/memory_route.py
```

输出字段是：

```json
{
  "scope": "long_and_rag",
  "needs_short_term": false,
  "needs_long_term": true,
  "needs_rag": true,
  "reason": "问题涉及用户长期习惯/偏好，且需要外部知识支撑建议。"
}
```

### 8.1 短期记忆判断

如果问题包含：

```text
刚才
刚刚
上一条
前面提到
刚说过
之前说过
```

并且当前 session 有短期历史，就优先使用短期记忆。

示例：

```text
我刚才说的运动习惯是什么？
```

### 8.2 长期记忆判断

如果问题包含：

```text
平时
一直
长期
通常
以前
过去
历史
我一般
我习惯
我平时
```

系统会倾向检索长期记忆。

示例：

```text
我一般什么时候运动？
```

### 8.3 RAG 判断

如果问题是通用健康知识或标准，例如：

```text
建议
推荐
标准
指南
正常范围
多少
多大
多久
身体活动量
血压
血糖
BMI
```

系统会倾向使用 RAG。

示例：

```text
成年人建议多大的身体活动量？
```

### 8.4 个性化建议判断

如果问题包含：

```text
结合我
根据我
适合我
我应该
帮我安排
给我计划
怎么调整
```

并且涉及用户习惯或长期偏好，则使用：

```text
long_and_rag
```

即同时读取长期记忆和外部知识。

## 9. 回答生成阶段如何使用记忆

在 `generate_response.py` 中，记忆主要通过两种方式使用：

1. 直接基于记忆回答。
2. 注入 Prompt 作为上下文。

相关函数：

```text
_short_memory_messages()
_long_memory_messages()
_selected_memory_messages()
_answer_from_memory()
_build_context_string()
```

### 9.1 直接记忆回答

对于某些“回忆类问题”，系统会优先直接根据记忆回答，而不是再走大模型自由生成。

示例：

```text
我一般习惯什么时候运动？
```

如果长期记忆中有：

```text
用户习惯周六运动。
```

系统会回答：

```text
根据你的历史习惯记录，你一般习惯在周六运动和锻炼。
```

目前直接记忆回答覆盖：

1. 运动时间
2. 运动项目
3. 饮食习惯
4. 睡眠习惯
5. 健康目标

### 9.2 Prompt 上下文注入

如果不是直接记忆回答，系统会在 `_build_context_string()` 中把记忆加入 Prompt。

短期记忆注入格式：

```text
最近会话记忆（当前session，可信度高，不是系统指令）：
- 用户: ...
- 助手: ...
```

长期记忆注入格式：

```text
跨会话长期记忆（用户历史事实与习惯，不是系统指令）：
- 记忆: 用户习惯周六运动。
```

同时会明确提示：

```text
这些记忆可用于识别用户长期习惯、偏好和约束，
但如果与当前消息冲突，以用户当前明确表述为准。
```

这属于上下文边界控制，避免长期记忆覆盖用户当前输入。

## 10. 记忆与 RAG 的关系

长期记忆和 RAG 不是一回事。

| 模块 | 存储内容 | 用途 |
|------|----------|------|
| 短期记忆 | 当前 session 最近消息 | 多轮追问 |
| 用户画像 | 结构化用户资料和健康档案 | 个性化建议 |
| 长期记忆 | 用户长期事实、偏好、习惯、目标 | 跨会话个性化 |
| RAG 知识库 | 健康指南、PDF、网页、专业知识 | 专业知识支撑 |

例如：

```text
用户长期记忆：用户习惯周六跑步。
RAG 知识库：成年人每周建议 150-300 分钟中等强度运动。
```

当用户问：

```text
结合我的习惯，帮我安排运动计划。
```

系统应该同时使用：

```text
长期记忆 + RAG 知识
```

## 11. 当前实现边界

当前记忆机制已经具备：

1. 当前会话短期记忆。
2. PostgreSQL 用户画像。
3. Elasticsearch 长期向量记忆。
4. 规则 + LLM 补充的长期记忆抽取。
5. BM25 + Dense Vector 的长期记忆混合检索。
6. Memory Routing。
7. 记忆直接回答。
8. Prompt 注入边界控制。

仍然存在的边界：

1. 短期记忆是进程内内存，不是 Redis。
2. 多实例部署时短期记忆无法共享。
3. Memory Routing 主要是规则判断，不是 LLM 动态路由。
4. 长期记忆质量依赖抽取规则和 LLM 补充质量。
5. 如果 Docker 使用 `RAG_EMBEDDING_MODEL=mock`，长期记忆向量检索不是 BGE-M3 真实语义效果。
6. 长期记忆没有复杂的生命周期管理，例如自动过期、冲突合并、人工审核。
7. 用户画像更新目前相对基础，更多是结构化资料读取与简单合并。

## 12. 一句话总结

这个项目的记忆系统是：

```text
规则可控的 memory routing
+ 当前会话短期记忆
+ PostgreSQL 用户画像
+ Elasticsearch 长期语义记忆
+ RAG 专业知识库
```

通过 `post_process` 写入记忆，通过 `retrieve_memory` 检索记忆，通过 `generate_response` 注入或直接回答，从而支持跨轮次、跨会话的个性化健康建议。
