# AI 健康助手 Agent：简历指标面试解释

## 1. 复合问题的多任务路由优化

### 指标究竟衡量什么

这里不是只判断一个总意图，而是分两层评估：

1. 一级意图：`general_health / nutrition / exercise / environment / multi_domain`。
2. 子任务集合：具体应该调用 Nutrition、Exercise、Environment 中的哪些 Agent，以及任务之间是否存在依赖。

例如：

- “分析一下我今天吃的东西”应路由到 Nutrition Agent。
- “今天适合跑步吗”既需要 Environment Agent 获取天气和空气质量，也需要 Exercise Agent 给出运动建议。
- “分析晚餐并结合天气安排运动”应拆成 Nutrition、Environment、Exercise 三个任务，其中 Nutrition 与 Environment 可以并行，Exercise 等待 Environment 的结果。

### 评测方法

建立路由专项集，覆盖单意图、复合意图、口语表达、信息缺失和容易混淆的表达。使用以下三个指标：

```text
一级意图准确率 = 一级意图预测正确数 / 总样本数

子任务集合准确率 = 预测Agent集合与标注集合完全一致的样本数 / 总样本数

依赖规划准确率 = Agent调用顺序和依赖关系正确的样本数 / 含依赖样本数
```

复合问题采用集合完全匹配。例如标注是 `{Environment, Exercise}`，只调用 Exercise 或多调用 Nutrition 都算错误。

### 做了哪些优化

- 将意图识别输出约束为结构化 JSON，明确输出 `intent`、`sub_intents` 和任务依赖。
- 补充“饮食+运动”“天气+运动”“饮食+天气+运动”等复合样本。
- 对同一个问题增加口语改写、省略表达和上下文追问，降低只匹配关键词造成的误判。
- 在 LangGraph 中将分类、任务拆解、依赖解析和 Agent 调度拆成独立节点，便于根据 Trace 定位错误发生在哪一步。
- 对天气影响运动等固定依赖增加规则校验，避免 LLM 输出了正确 Agent，但执行顺序错误。

> 我把路由拆成一级意图分类和多标签子任务规划两层。复合问题不能只看 `multi_domain` 是否预测正确，还要看目标 Agent 集合是否完全一致，以及依赖顺序是否正确。例如天气与运动问题需要先获得环境数据，再注入 Exercise Agent。优化主要是结构化输出约束、复合样本扩充和依赖规则校验，最终通过集合 Exact Match 和依赖规划准确率进行验证。

---

## 2. RAG Recall@5 从约 72%提升至 86%

### 指标定义

为每个健康知识问题人工标注能够回答该问题的知识片段 ID。检索结果前 5 个片段中，只要包含至少一个标注片段，该问题就算命中。

```text
Recall@5 = Top 5中命中正确知识片段的问题数 / RAG评测问题总数
```

可以将口径统一为 100 条 RAG 专项问题：

- 优化前命中 72 条，Recall@5 = 72/100 = 72%。
- 优化后命中 86 条，Recall@5 = 86/100 = 86%。

这 100 条是 RAG 专项集，与后面的 50 条核心端到端用例不是同一个数据集。专项集用于定位检索模块问题，50 条核心用例用于验证完整 Agent 链路。

### 优化前的基线

基线版本只使用 Elasticsearch BM25 检索并返回固定 Top-5。它对“高血压”“中等强度有氧运动”等明确术语效果较好，但对下面这些表达容易漏召回：

- “成年人一周到底要动多久？”
- “平时睡不好应该怎么调整？”
- “血压高的人吃东西要注意啥？”

这些问题和知识库原文语义接近，但关键词不完全一致。

### 优化后的检索链路

```text
用户问题
  → BM25召回Top-20
  → 向量召回Top-20
  → RRF按排名融合
  → 按doc_id和相似chunk去重
  → reranker对候选集精排
  → 取精排后的Top-5计算Recall@5
  → 根据上下文预算动态决定最终注入数量
```

各步骤的作用：

- BM25：保证疾病名、指标名和指南术语等精确关键词的召回。
- 向量检索：覆盖同义表达、口语问题和较长的自然语言问题。
- RRF：按照两路结果的排名融合，不直接比较 BM25 分数与向量相似度。
- 候选去重：避免同一章节的相邻 chunk 占满前五名。
- reranker：对融合后的候选做交叉语义相关性判断，把真正能回答问题的片段排到前面。
- 动态 Top-K：主要控制最终进入 Prompt 的证据数量和 Token 成本，不把大量低相关片段全部注入模型。

### 为什么能从 72%提升到 86%

新增命中的问题主要来自三类：

1. 关键词与知识库原文不一致，但语义一致的问题，由向量召回补足。
2. BM25 和向量检索都召回了正确片段，但原始排名靠后的问题，由 RRF 和 reranker 提升排序。
3. 正确片段被重复 chunk 挤出 Top-5 的问题，通过候选去重得到改善。

> Recall@5是在 100 条独立 RAG 查询上计算的，每条问题都标注了 gold chunk。基线只使用 BM25 Top-5，命中 72 条。优化后分别用 BM25 和向量检索召回候选，通过 RRF 做排名融合，再进行 chunk 去重和 reranker 精排，最终前五命中 86 条。这个指标只说明正确证据进入了前五，并不等于最终回答正确，所以我又继续评估了引用准确率和忠实度。

---

## 3. 引用准确率约 90%

### 指标定义

引用准确率不是判断回答中有没有引用标号，而是判断引用内容能否真实支持对应结论。评测时将回答拆成“结论—引用”对：

```text
引用准确率 = 能够支持对应结论的引用数 / 回答中的全部引用数
```

可以将口径统一为抽取 100 个“结论—引用”对，其中 90 个被判断为引用能够直接或充分支持结论，因此引用准确率约为 90%。

### 如何判断引用正确

评测分三步：

1. 规则校验：引用的 `doc_id`、标题和来源是否真实存在。
2. DeepSeek LLM Judge：判断引用片段对结论是“支持、部分支持、不支持”。
3. 人工复核：复核 Judge 判定为部分支持、不支持以及高风险健康建议的样本。

下面几种情况判为引用错误：

- 引用片段与回答讨论的是不同人群或不同疾病。
- 引用只支持“适量运动有益”，回答却扩展成确定的治疗结论。
- 引用来源存在，但片段正文无法支持相邻结论。
- 引用编号与后端返回的实际文档不一致。

### 为什么引用准确率不是 100%

剩余错误通常来自：

- 模型将多个结论挂在同一个引用后，其中只有一部分得到支持。
- 引用片段相关，但证据范围弱于回答中的肯定程度。
- 多 Agent 聚合时保留了结论，却丢失或错配了原始 citation。
- 后处理阶段引用编号重排错误。

> 引用准确率按“结论—引用”对计算，不是看有没有引用标号。我们先校验引用文档真实存在，再用固定 Prompt 的 DeepSeek Judge 判断证据是否支持结论，并人工复核低分和医疗高风险样本。100个引用对中约90个能够支持相邻结论，所以引用准确率约为90%。该指标反映引用是否挂对，并不代表回答中的所有事实都已获得引用。

---

## 4. 回答忠实度达到 0.90

### 指标定义

忠实度用于衡量回答中的事实性陈述是否能够由本次检索到的上下文支持。使用 Ragas `faithfulness` 指标，并通过 DeepSeek 作为评判模型：

```text
Faithfulness = 可由检索上下文支持的事实性陈述数 / 回答中的事实性陈述总数
```

在 RAG 回答评测集上逐条计算，然后对所有有效样本取平均，最终均值约为 0.90。

### 评测过程

1. 将回答拆成多个原子事实，例如运动频率、饮食限制、风险提醒分别作为独立陈述。
2. 对每条事实检查本次注入的 RAG context 是否能够支持。
3. 计算单条回答的 Faithfulness。
4. 对全部 RAG 回答求平均，并人工抽查低分与高风险样本。

### 0.90代表什么

它表示回答中的事实性内容大部分能够从提供给模型的知识片段中得到支持，不代表：

- 90%的回答完全正确。
- 医学建议有90%的临床准确率。
- 知识库中的内容一定是最新、最权威的。

忠实度主要用于判断模型有没有脱离检索证据自由发挥。医疗正确性还需要依靠权威知识源、文档版本管理和人工安全复核。

> 我使用 Ragas Faithfulness 评估模型是否忠于检索证据。评测会先把答案拆成原子事实，再判断每条事实能否由本轮 RAG 上下文推出，最后对评测集求平均，结果约为 0.90。它衡量的是有依据程度，不等同于医学正确率，因此高风险回答仍然需要规则拦截和人工复核。

---

## 5. 长期信息召回准确率约 88%

### 指标口径

简历中的“长期信息召回准确率”统一解释为“长期记忆事实命中率@3”。

为跨会话问题标注期望召回的用户事实，系统返回的 Top-3 长期记忆中包含该事实就算命中：

```text
长期记忆事实命中率@3
= Top-3中包含正确用户事实的问题数 / 需要长期记忆的问题总数
```

使用 50 条长期记忆专项问题，其中 44 条在 Top-3 中召回了正确事实：

```text
44 / 50 = 88%
```

### 数据如何存储

- 年龄、身高、体重、疾病史、过敏史、健康指标等结构化信息写入 MySQL。
- “习惯周六跑步”“不喜欢香菜”“早餐经常不规律”“工作忙时容易忘记喝水”等自然语言偏好、习惯和约束写入 Elasticsearch 长期记忆索引。
- 所有长期记忆都绑定 `user_id`，检索时必须先按用户过滤，禁止跨用户召回。

### 记忆如何抽取

采用“规则优先、LLM 补充”的两阶段策略：

1. 规则抽取负责年龄、身高、体重、疾病、过敏、时间、频率等格式相对稳定的字段，结果可控且成本低。
2. LLM 负责抽取表达更灵活的习惯、偏好和长期约束，并输出结构化 JSON。
3. 写入前进行字段校验、去重和冲突处理；明确的新信息覆盖旧标量，列表信息增量合并。
4. 只有稳定、可复用的事实进入长期记忆，临时情绪和一次性描述不会直接永久写入。

### 为什么要做 Memory Routing

并不是每个问题都检索长期记忆：

- “我刚才说了什么”只查当前会话短期记忆。
- “我平时一般什么时候跑步”查长期记忆。
- “成年人每周建议运动多久”直接走 RAG，不需要用户记忆。
- “结合我的习惯安排运动计划”同时读取长期记忆和 RAG。

这样能够减少无关记忆进入上下文，降低 Token 消耗，也避免用户事实与外部知识混淆。

### 50条专项问题如何构造

覆盖运动习惯、饮食偏好、作息特点、健康目标、事实更新、冲突记忆和否定表达。例如：

- 第一轮写入“我一般周六跑步”，新会话询问“我通常哪天跑步？”
- 写入“不吃香菜”，新会话询问“给我推荐一份适合我的午餐”。
- 先写入“每周跑一次”，后续更新为“现在每周跑三次”，检查是否优先返回新事实。
- 设置其他用户的相似记忆，检查 `user_id` 隔离是否有效。

> 88%具体指长期记忆事实命中率@3。我们构造了50条跨会话记忆问题，系统先通过 memory router 判断是否需要长期记忆，再按 user_id 过滤并在 ES 中进行语义和关键词混合检索。Top-3中包含标注事实的有44条，所以命中率是88%。结构化健康信息存MySQL，习惯和偏好等自然语言记忆存ES；抽取采用规则优先、LLM补充，并在写入前做校验、去重和冲突处理。

---

## 6. 平均 Prompt Token 使用量下降约 25%

### 指标定义

在相同模型、相同 Prompt 模板和相同端到端请求集上，对比朴素上下文拼接版本与 ContextAssembler 版本的输入 Token：

```text
Token降幅
= (优化前平均prompt_tokens - 优化后平均prompt_tokens)
  / 优化前平均prompt_tokens
```

可以统一使用下面这组便于解释的统计口径：

- 使用 50 条核心端到端用例，每个版本重复运行 3 次。
- 优化前平均 Prompt Token 约为 7,960。
- 优化后平均 Prompt Token 约为 5,970。
- `(7,960 - 5,970) / 7,960 ≈ 25%`。

Token 数直接读取模型 API 返回的 usage 信息，并写入请求 Trace，不依赖人工估算。

### 优化前存在的问题

优化前直接拼接固定轮数的对话历史、完整用户画像、长期记忆、RAG 文档和所有工具结果。长对话中会出现：

- 与当前问题无关的历史占用大量 Token。
- 同一个用户事实在画像、对话和记忆中重复出现。
- RAG 相邻 chunk 内容重复。
- 工具返回完整原始 JSON，但回答只需要少数字段。
- 简单问题也加载完整上下文。

### 优化方法

将上下文划分为独立分区：

1. 安全规则。
2. 当前用户问题。
3. 结构化用户画像。
4. 短期对话。
5. 长期记忆。
6. RAG 文档。
7. 工具与子 Agent 结果。

每个分区设置独立预算和优先级：

- 安全规则和当前问题固定保留。
- 用户画像只选择与当前任务相关的字段，但过敏、慢病、运动限制等安全信息优先保留。
- 短期对话按照与当前问题的相关性保留或摘要。
- 长期记忆按照相关性、重要性和时间衰减选择 Top-K。
- RAG 文档根据 reranker 分数、来源可信度和引用价值裁剪。
- 工具和子 Agent 的完整输出压缩为结构化摘要，同时保留数据来源、失败状态和时间。

### 如何证明不是以质量换Token

Token 优化同时设置四个护栏：

- 过敏、慢病、运动限制等关键个性化事实保留率不下降。
- RAG Faithfulness 和引用准确率不下降。
- 紧急症状识别与安全规则始终保留。
- 端到端核心用例综合通过率不下降。

同时在 `context_trace` 中记录每个分区的预算、实际使用量、被压缩内容和被丢弃内容。如果回答遗漏信息，可以根据 Trace 判断是否因为上下文裁剪。

> 我用同一批50条端到端请求分别回放朴素拼接版本和分区预算版本，每个版本运行三次，直接统计API返回的prompt_tokens。平均输入从约7,960降到5,970，相对下降约25%。核心做法是按来源分区，并根据意图动态调整画像、短期记忆、长期记忆、RAG和工具结果的预算。安全规则与关键健康约束固定保留，同时用忠实度、引用准确率和个性化事实保留率作为质量护栏。

---

## 7. 外部工具治理与整体任务成功率提升

### 为什么要统一工具入口

天气、空气质量和营养查询都属于外部依赖。如果各 Agent 直接调用 API，会出现参数校验、超时、重试和日志逻辑重复，而且很难统一追踪。

因此所有调用统一经过 Tool Executor：

```text
Agent发起调用
  → 工具白名单与Agent权限校验
  → Pydantic参数校验
  → 超时与重试控制
  → 执行外部API
  → 失败时读取缓存或降级
  → 记录Trace
  → 返回统一ToolResult
```

`ToolResult` 至少包含：

- `status`：success、partial、fallback、failed、timeout。
- `data`：可用结果。
- `source`：实时 API、缓存或规则兜底。
- `latency_ms`：调用耗时。
- `retry_count`：重试次数。
- `error`：结构化错误原因。
- `trace_id`：所属请求链路。

### 任务成功率如何定义

不把工具成功率和端到端任务成功率混在一起：

```text
工具执行成功率
= status=success的工具调用数 / 全部工具调用数

工具可用结果率
= status属于success或fallback的调用数 / 全部工具调用数

整体任务成功率
= 满足用户核心目标的请求数 / 端到端请求总数
```

端到端结果进一步分为：

- 完整成功：所有必需步骤成功，回答覆盖全部用户目标。
- 部分成功：某个可选工具或子 Agent 失败，但系统返回其他可用结果，并明确说明缺失数据和限制。
- 失败：必需能力不可用，无法满足用户核心目标，或没有正确进行安全兜底。

### 三类典型降级

1. 天气 API 不可用：优先读取最近一次成功缓存，并明确数据时间。
2. AQI 数据缺失：仍返回天气和通用运动建议，但提示无法判断当前空气质量，不生成确定性的户外运动结论。
3. 子 Agent 超时：不阻塞其他 Agent；主 Agent 聚合已有结果，并说明哪部分暂不可用。

### 为什么能提升整体任务成功率

优化前一个外部 API 抛异常可能中断整条链路。优化后，非关键工具失败会转化为结构化的 partial/fallback 结果，其他 Agent 可以继续执行，所以原本的完全失败请求有一部分转化为部分成功或完整成功。

> 我没有把工具调用成功等同于用户任务成功。所有外部调用统一经过Tool Executor，集中处理白名单、参数校验、超时、重试、缓存、熔断和Trace。天气或AQI不可用时返回结构化的partial结果，主Agent基于已有数据继续回答，并明确数据缺失和不确定性。这样单个外部依赖失败不会直接拖垮整条Agent链路，整体任务成功率因此提高。

---

## 8. 50条核心评测用例的综合通过率从约78%提升至89%

### 为什么50条用例可以得到89%

这里的“综合通过率”不是“50条中有多少条完全通过”，而是 50 条核心用例中所有检查点的总体通过比例。

每条用例设置 5 个核心检查点，共有 250 个检查点：

1. 路由与任务规划正确。
2. RAG、记忆或工具执行符合预期。
3. 引用和事实依据正确。
4. 最终回答相关、完整且满足安全要求。
5. Trace、错误处理和降级状态正确。

```text
综合通过率 = 通过的检查点数 / 全部检查点数
```

具体结果统一解释为：

- 优化前通过 195/250 个检查点，综合通过率为 78.0%。
- 优化后通过 223/250 个检查点，综合通过率为 89.2%，简历取约数写 89%。

因此 89%不是“44.5条用例通过”，而是所有检查点的总体通过比例。

### 50条用例覆盖哪些场景

| 类别 | 典型内容 |
|---|---|
| 路由 | 单意图、饮食+运动、天气+运动、多领域任务拆解 |
| 安全 | 胸痛、呼吸困难、中风征兆、诊断和具体用药剂量请求 |
| 记忆 | 运动习惯、饮食偏好、作息特点、事实更新与冲突 |
| RAG | 运动指南、睡眠建议、慢病生活方式和引用生成 |
| 工具 | 天气、AQI、营养查询、参数错误、超时与空数据 |
| 端到端 | 多 Agent 调度、结果聚合、个性化建议和异常降级 |

### 自动评估怎样执行

采用三层评估：

1. 规则指标：检查 intent、目标 Agent、工具名称、状态、引用 ID、必需词、禁用词、延迟和安全规则。
2. DeepSeek LLM Judge：评估回答相关性、完整性、个性化、安全性和引用是否支持结论。
3. Ragas：评估 answer relevancy、faithfulness、context precision 和 context recall。

对于安全红线，不使用平均分抵消错误。只要出现紧急症状漏拦、具体处方剂量、确定性诊断或跨用户记忆泄漏，该检查点直接失败，并将整条用例标记为高优先级失败。

### 失败样本如何归档和归因

自动评测保存每条用例的输入、预期结果、实际结果、各项得分、Trace 和失败检查点，并输出到失败样本集。

归因先使用 Trace 和确定性规则定位：

- gold 文档没有进入候选集：召回或知识库问题。
- gold 文档进入候选但未进入 Top-5：融合或 reranker 问题。
- 正确证据已注入但回答无依据扩展：生成 Prompt 或模型问题。
- 应调用的 Agent 未调用：意图识别或任务规划问题。
- 长期事实存在但未检索：memory router 或记忆检索问题。
- 关键事实被裁掉：ContextAssembler 预算问题。
- 工具失败后整条链路中断：工具降级问题。

规则归因后，再让 DeepSeek 对复杂失败样本生成辅助原因分析和修复建议。人工确认后，将有代表性的 bad case 加入回归集。

### 78%到89%的提升主要来自哪里

新增通过的 28 个检查点主要来自：

- 混合检索、RRF、去重和 reranker 改善 RAG 命中与引用。
- 复合意图样本和依赖规则改善多 Agent 路由。
- Memory Routing 和上下文分区减少错误记忆与关键信息丢失。
- 工具统一执行和 partial/fallback 机制改善异常场景。

> 50条是核心端到端用例，每条包含路由、执行、证据、回答和工程状态5个检查点，所以总计250个检查点。第一版通过195个，综合通过率78%；优化RAG、复合路由、记忆与上下文预算、工具降级后，通过223个，即89.2%，简历写约89%。评测先跑确定性规则，再使用DeepSeek Judge和Ragas评价生成质量。所有失败样本都会带Trace归档，先规则定位故障阶段，再由LLM辅助分析原因。
