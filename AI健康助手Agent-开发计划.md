# AI健康助手Agent - 新手分阶段开发计划

## 1. 开发目标

本计划面向第一次独立开发该项目的新手,目标是实现一个**可上线演示、功能完整、具备安全治理和工程可维护性**的AI健康助手Agent系统。

最终版本应包含:
- 用户登录与基础鉴权
- 文本对话式AI健康助手
- 用户健康档案管理
- 健康顾问Agent、营养Agent、运动Agent、环境Agent
- RAG健康知识检索与引用回答
- 记忆系统与用户画像
- ContextAssembler上下文预算与压缩
- Prompt注入防护与工具调用安全治理
- ToolExecutor统一工具执行入口
- 全链路可观测性
- Agent效果评估体系
- 前后端完整联调、测试与部署

以下能力先不实现,作为后续增强方向:
- 多模态图片识别
- 配置热更新
- 系统化版本管理与回归评估
- 复杂服务拆分

## 2. 推荐开发节奏

| 阶段 | 主题 | 目标产出 |
|------|------|----------|
| Phase 0 | 项目理解与环境准备 | 本地环境跑通,理解核心流程 |
| Phase 1 | 后端基础框架 | FastAPI服务、配置、数据库连接 |
| Phase 2 | 用户、鉴权与健康档案 | 用户体系、健康档案、健康数据CRUD |
| Phase 3 | 基础聊天与LLM接入 | 文本聊天API、会话历史、LLM调用 |
| Phase 4 | 记忆系统与用户画像 | 短期记忆、长期画像、ContextAssembler上下文预算 |
| Phase 5 | Agent核心能力 | 主Agent与3个专业Agent协作 |
| Phase 6 | RAG知识库 | 文档入库、检索、带引用回答 |
| Phase 7 | 安全治理 | 防注入、医疗安全边界、ToolExecutor工具安全 |
| Phase 8 | 可观测性 | Trace ID、结构化日志、tool_trace/context_trace |
| Phase 9 | Agent评估体系 | 评测集、自动评估、人工评审流程 |
| Phase 10 | 前端H5 | 聊天页、档案页、导入页、数据可视化 |
| Phase 11 | 联调、测试与部署 | 可交付版本、测试报告、部署说明 |
| Phase 12 | 暂缓增强功能 | 多模态、热更新、版本管理、服务拆分 |

## 3. Phase 0: 项目理解与环境准备

### 3.1 目标

理解项目不是“一个聊天机器人”,而是“前端页面 + 后端API + Agent编排 + 知识库 + 用户数据 + 安全治理 + 可观测性 + 评估体系”的组合系统。

### 3.2 具体步骤

1. 阅读技术方案文档,重点理解:
   - 项目目标
   - 系统架构
   - Agent分工
   - 数据存储设计
   - 安全性设计
   - 评估指标

2. 准备本地环境:
   - Python 3.11+
   - Node.js 18+
   - PostgreSQL
   - Redis
   - Elasticsearch
   - Git
   - VS Code或Cursor

3. 创建环境变量文件:
   - 数据库连接地址
   - Redis地址
   - Elasticsearch地址
   - LLM API Key
   - JWT密钥
   - 外部API Key

4. 跑通最小服务:
   - 后端能启动
   - 前端能启动
   - 浏览器能访问首页
   - `/health`接口返回正常

### 3.3 验收标准

- 可以在本地启动后端服务
- 可以在本地启动前端页面
- 知道一次用户提问从前端到Agent回复经过哪些模块
- 能说明哪些能力属于当前开发主线,哪些能力暂缓

## 4. Phase 1: 后端基础框架

### 4.1 目标

搭建稳定的FastAPI后端基础,让后续业务、安全、观测和评估模块都有统一入口。

### 4.2 具体步骤

1. 建立后端目录结构:
   - `app/main.py`
   - `app/config.py`
   - `app/api/`
   - `app/models/`
   - `app/schemas/`
   - `app/services/`
   - `app/agents/`
   - `app/rag/`
   - `app/security/`
   - `app/observability/`
   - `app/evaluation/`

2. 实现配置管理:
   - 使用Pydantic Settings读取环境变量
   - 区分开发环境和生产环境配置
   - 当前阶段采用“修改配置后重启服务”的方式
   - 不实现配置热更新

3. 实现基础中间件:
   - CORS
   - 请求日志
   - Trace ID
   - 全局异常处理
   - 请求耗时统计

4. 实现健康检查接口:
   - `GET /health`
   - 返回服务状态、数据库状态、Redis状态、Elasticsearch状态

5. 接入数据库:
   - SQLAlchemy 2.0
   - Alembic迁移
   - PostgreSQL连接池

### 4.3 验收标准

- `GET /health`可用
- 数据库、Redis、Elasticsearch连接状态可检查
- 启动服务时配置错误能明确报错
- 基础异常返回格式统一
- 每个请求都有Trace ID

## 5. Phase 2: 用户、鉴权与健康档案

### 5.1 目标

建立用户体系和健康数据基础,为个性化Agent回答提供可靠上下文。

### 5.2 具体步骤

1. 用户模块:
   - 用户注册
   - 用户登录
   - JWT签发与校验
   - 获取当前用户信息
   - 密码哈希存储

2. 权限控制:
   - 后端从Token解析用户ID
   - 所有用户数据接口必须校验数据归属
   - 管理类接口预留管理员角色

3. 健康档案模块:
   - 基础资料: 年龄、性别、身高、体重
   - 健康目标: 减脂、增肌、控糖、降压等
   - 慢病信息: 高血压、糖尿病等
   - 生活习惯: 饮食、运动、睡眠
   - 风险提示字段: 过敏史、用药信息、禁忌说明

4. 健康数据模块:
   - 运动数据
   - 睡眠数据
   - 心率数据
   - 体重记录

5. 数据导入:
   - 支持CSV导入
   - 校验字段格式
   - 返回导入成功和失败条数
   - 保存导入批次记录

### 5.3 验收标准

- 用户可以注册、登录
- 登录后可以新增和修改健康档案
- 可以查询自己的健康数据
- 不能访问其他用户的数据
- 用户敏感字段不会出现在普通日志中

## 6. Phase 3: 基础聊天与LLM接入

### 6.1 目标

完成“用户提问 -> 后端构造上下文 -> 调用LLM -> 保存会话 -> 返回回答”的完整闭环。

### 6.2 具体步骤

1. 设计聊天数据表:
   - 会话表
   - 消息表
   - 消息角色: user、assistant、system
   - 创建时间
   - Token消耗
   - Trace ID
   - 安全处理结果

2. 实现聊天接口:
   - `POST /api/v1/chat`
   - `GET /api/v1/chat/sessions`
   - `GET /api/v1/chat/sessions/{id}/messages`

3. 接入LLM Provider:
   - 封装统一调用类
   - 支持超时
   - 支持失败重试
   - 记录Token消耗
   - 不把API Key写死在代码里

4. 加入基础医疗安全提示:
   - 健康建议不能替代医生诊断
   - 遇到紧急症状提示立即就医
   - 避免输出具体处方和药量

### 6.3 验收标准

- 用户可以发送文本消息
- 系统可以返回AI回复
- 会话历史可以保存和查看
- LLM调用失败时有友好的错误提示
- 每次LLM调用记录模型、耗时、Token和Trace ID

## 7. Phase 4: 记忆系统与用户画像

### 7.1 目标

让Agent具备连续服务能力,能够结合用户历史、健康目标和长期偏好生成个性化建议。

### 7.2 具体步骤

1. 短期记忆:
   - 当前会话最近N轮消息
   - 当前问题的临时上下文
   - 会话级摘要

2. 长期用户画像:
   - 健康目标
   - 慢病信息
   - 饮食偏好
   - 运动偏好
   - 风险提示

3. 记忆写入策略:
   - 从对话中提取稳定事实
   - 过滤敏感和不确定信息
   - 用户明确纠正时覆盖旧画像

4. 记忆读取策略:
   - 根据问题类型读取相关画像
   - 控制上下文长度
   - 避免把无关历史全部塞进Prompt

5. ContextAssembler上下文预算:
   - 将用户画像、短期记忆、长期记忆、RAG文档、健康档案摘要、子Agent结果分区组装
   - 为每个分区设置默认Token预算
   - 根据 `memory_policy` 动态调整短期记忆、长期记忆和RAG预算
   - 通用知识问题优先保留RAG,弱化短期记忆
   - 当前会话追问优先保留短期记忆,可以跳过RAG

6. 短期记忆压缩:
   - 当前阶段先保留最近N轮作为候选
   - 最终进入Prompt前由ContextAssembler按预算截断
   - 后续可扩展rolling summary,把长会话压缩为会话摘要
   - 记录被截断、压缩、丢弃的上下文片段

### 7.3 验收标准

- 用户画像可以从档案和对话中更新
- Agent回答能体现用户目标和健康限制
- 用户纠正信息后,后续回答使用新信息
- 记忆写入有日志,便于排查错误画像
- `context_trace` 可以看到各上下文分区Token用量
- 短期记忆不会无差别全部塞入Prompt
- RAG问题、短期追问、长期习惯回忆能使用不同上下文预算策略

## 8. Phase 5: Agent核心能力

### 8.1 目标

把基础LLM聊天升级为“主Agent + 专业Agent协作”的健康管理系统。

### 8.2 具体步骤

1. 定义Agent统一接口:
   - `name`
   - `description`
   - `can_handle(intent)`
   - `run(input, context)`
   - `required_tools`
   - `safety_level`

2. 实现Agent注册中心:
   - 注册健康顾问Agent
   - 注册营养Agent
   - 注册运动Agent
   - 注册环境Agent
   - 记录Agent启用状态

3. 实现健康顾问Agent:
   - 识别用户意图
   - 读取用户画像
   - 判断是否需要RAG
   - 决定是否调用子Agent
   - 聚合结果并生成最终回复

4. 实现营养Agent:
   - 处理文本饮食输入
   - 从用户描述中提取食物
   - 查询营养数据
   - 计算热量和主要营养成分
   - 结合用户目标生成饮食建议

5. 实现运动Agent:
   - 读取用户目标和身体信息
   - 结合历史运动数据
   - 生成运动建议
   - 给出运动强度提醒
   - 避免对高风险用户给出激进计划

6. 实现环境Agent:
   - 接入天气或空气质量API
   - 根据位置返回环境健康提醒
   - API失败时给出降级回复

7. 实现Agent编排:
   - 意图识别
   - 任务规划
   - 子Agent调用
   - 结果聚合
   - 失败降级
   - 子Agent返回统一 `AgentResponse`
   - 聚合子Agent的 `tool_trace`、warnings 和 partial 状态

### 8.3 验收标准

- 不同类型问题能路由到不同Agent
- 主Agent能把多个子Agent结果整合成一段自然回复
- 任一子Agent失败时不会导致整个聊天接口崩溃
- Agent执行过程可以通过Trace ID追踪
- 子Agent调用过的工具能在主Agent侧统一看到调用轨迹

## 9. Phase 6: RAG知识库

### 9.1 目标

让健康建议尽量基于可信知识来源,减少LLM幻觉,并让用户看到回答依据。

### 9.2 具体步骤

1. 准备知识文档:
   - 健康饮食指南
   - 运动建议资料
   - 慢病管理资料
   - 常见健康问答
   - 医疗免责声明模板

2. 实现文档处理:
   - 文档切分
   - 元数据标注
   - 向量生成
   - 写入Elasticsearch

3. 实现检索流程:
   - 用户问题向量化
   - Top-K召回
   - 混合检索
   - 简单重排序
   - 返回引用片段
   - 初召回K适当放大,最终注入K受ContextAssembler预算控制
   - 记录召回数量、注入数量和被截断文档

4. 接入健康顾问Agent:
   - 判断是否需要检索
   - 将检索结果加入Prompt
   - 回复中给出引用来源
   - 检索不足时明确说明依据有限
   - RAG片段作为只读外部资料分区注入,不能覆盖系统规则

### 9.3 验收标准

- 可以把一批健康知识导入知识库
- 用户问专业健康问题时能检索到相关片段
- 回复中能看到引用来源
- 检索不到资料时不会编造来源
- RAG检索耗时和命中情况可记录
- `context_trace` 中可以看到 RAG 文档分区的预算、实际用量和截断情况

## 10. Phase 7: 安全治理

### 10.1 目标

让系统具备基础防攻击、防误用、防越界能力,尤其要处理Prompt注入、工具误调用和医疗安全边界。

### 10.2 具体步骤

1. 输入安全分类:
   - 普通健康咨询
   - 紧急症状
   - 用药/诊断高风险问题
   - Prompt注入疑似输入
   - 越权或攻击性输入

2. Prompt注入防护:
   - 用户输入、RAG片段、工具结果分区拼接
   - 明确标记外部内容不可信
   - 检测“忽略系统指令”等常见攻击句式
   - 对命中风险的请求降级处理

3. RAG内容安全:
   - 检索片段只作为参考资料
   - 禁止外部知识覆盖系统规则
   - 对引用来源和回答内容做一致性检查

4. 工具调用安全治理:
   - 建立 ToolRegistry,统一注册工具名称、描述、参数Schema
   - 建立 ToolPolicy,维护Agent到工具的白名单和最小权限
   - 建立 ToolExecutor,所有工具调用必须经过统一执行入口
   - 参数校验
   - 超时控制
   - 失败重试
   - 降级兜底
   - 统一返回 `ToolResult`
   - 统一记录 `tool_trace`
   - 高风险工具默认不可调用

5. 医疗安全边界:
   - 紧急症状优先提示就医
   - 不输出诊断结论
   - 不给出处方药剂量
   - 所有健康建议附带适度免责声明

6. 安全日志:
   - 记录安全分类结果
   - 记录拦截和降级原因
   - 脱敏保存用户输入摘要

### 10.3 验收标准

- Prompt注入样例不会覆盖系统规则
- 紧急症状会触发就医提示
- 工具参数非法时不会执行调用
- Agent不能调用白名单之外的工具
- 工具调用失败时返回结构化失败信息,而不是直接中断主流程
- 外部API失败时有降级回复
- 安全拦截和降级都有Trace ID可查

## 11. Phase 8: 可观测性

### 11.1 目标

让一次健康咨询请求在网关、编排层、子Agent、工具层、LLM层都可追踪,方便排查问题、优化性能和控制成本。

### 11.2 具体步骤

1. Trace ID:
   - 网关生成全局Trace ID
   - 写入响应Header
   - 贯穿Agent、工具、LLM调用

2. 结构化日志:
   - 使用JSON日志
   - 包含Trace ID、用户ID哈希、会话ID、Agent名
   - 敏感字段脱敏

3. 指标采集:
   - 接口响应耗时
   - LLM首字延迟和总耗时
   - Token消耗
   - Agent节点耗时
   - 工具调用成功率
   - 工具超时率、降级率、白名单拦截次数
   - RAG召回耗时
   - Context分区Token占比
   - 短期记忆/RAG文档/长期记忆截断率
   - 安全拦截率

4. 链路追踪:
   - LangGraph每个节点写Span日志
   - 工具调用和LLM调用挂载父Span
   - 形成完整调用树
   - `tool_trace` 记录 tool_name、status、latency_ms、source、retry_count
   - `context_trace` 记录 section、budget_tokens、used_tokens、truncated

5. 告警规则:
   - 错误率过高
   - LLM响应过慢
   - 外部工具超时率过高
   - 安全拦截率异常升高

### 11.3 验收标准

- 任意一次用户请求都能通过Trace ID查到完整执行链路
- 可以看到每个Agent、工具、LLM调用的耗时
- 可以统计Token消耗和接口错误率
- 可以看到每次Prompt中画像、短期记忆、长期记忆、RAG、工具结果分别占用了多少Token
- 日志中不直接暴露用户敏感健康数据

## 12. Phase 9: Agent评估体系

### 12.1 目标

建立一套不依赖主观感觉的Agent质量评估方法,覆盖意图识别、工具调用、RAG引用、安全边界和回复质量。

> 当前阶段只建设基础评估体系,不做系统化版本管理与回归评估。

### 12.2 具体步骤

1. 构建评测样本集:
   - 通用健康咨询
   - 营养分析
   - 运动建议
   - 环境健康提醒
   - 慢病风险咨询
   - Prompt注入样例
   - 紧急症状样例

2. 定义自动评估指标:
   - 意图分类准确率
   - Agent路由准确率
   - 工具调用成功率
   - 工具白名单拦截正确率
   - 工具降级可用率
   - RAG命中率
   - 引用准确率
   - 上下文分区预算是否符合预期
   - 关键记忆是否被保留
   - 无关短期记忆是否被压缩或跳过
   - Prompt注入拦截率
   - 紧急症状召回率
   - 响应延迟
   - Token成本

3. 定义人工评审维度:
   - 回复是否自然、易懂、有同理心
   - 建议是否结合用户画像
   - 是否越界诊断或给药
   - 多Agent聚合后的答案是否连贯
   - 免责声明是否合适

4. 实现评估脚本:
   - 批量读取评测样本
   - 调用Agent工作流
   - 收集结构化结果
   - 输出CSV或Markdown评估报告

5. 建立失败Case回流:
   - 记录低分样例
   - 标注失败原因
   - 补充到评测集中

### 12.3 验收标准

- 可以一键运行评测脚本
- 可以输出核心指标报告
- 至少覆盖30-50条评测样本
- 安全类样例单独统计结果
- 记忆类样例能检查短期记忆、长期记忆和RAG的路由与预算是否合理
- 工具类样例能检查白名单、失败降级和 `tool_trace`
- 失败Case能沉淀为后续优化依据

## 13. Phase 10: 前端H5开发

### 13.1 目标

实现一个可以真实使用的H5界面,覆盖登录、聊天、档案、导入、数据展示和错误反馈。

### 13.2 具体步骤

1. 搭建前端项目:
   - Vue 3
   - TypeScript
   - Vite
   - Element Plus
   - Pinia
   - Vue Router

2. 实现登录页:
   - 登录表单
   - 注册入口
   - Token保存
   - 未登录拦截

3. 实现聊天页:
   - 消息列表
   - 输入框
   - 发送按钮
   - 加载状态
   - 错误提示
   - 会话历史侧边栏
   - 引用来源展示
   - 安全提示展示

4. 实现健康档案页:
   - 基础资料表单
   - 健康目标选择
   - 慢病信息维护
   - 风险提示字段
   - 保存成功提示

5. 实现健康数据导入页:
   - CSV上传
   - 字段校验提示
   - 导入结果展示

6. 实现数据可视化:
   - 体重趋势
   - 步数趋势
   - 睡眠趋势
   - 心率趋势

7. 实现基础可观测性展示:
   - 开发环境展示Trace ID
   - 接口失败时可复制错误编号

### 13.3 验收标准

- 用户可以从登录到聊天完整走通
- 页面刷新后登录状态仍然有效
- 聊天失败、接口超时、表单错误都有提示
- 引用来源和安全提示能正确展示
- 移动端宽度下页面不明显错位

## 14. Phase 11: 联调、测试与部署

### 14.1 目标

把项目从“功能完成”推进到“稳定可交付”。

### 14.2 具体步骤

1. 后端测试:
   - 配置测试
   - 用户接口测试
   - 健康档案测试
   - 聊天接口测试
   - Agent路由测试
   - RAG检索测试
   - 安全治理测试
   - ToolExecutor白名单与降级测试
   - ContextAssembler预算与截断测试
   - 可观测性字段测试
   - 评估脚本测试

2. 前端联调:
   - 登录流程
   - 档案保存
   - 聊天发送
   - 会话历史
   - 引用展示
   - 数据导入
   - 错误提示

3. 异常场景验证:
   - LLM超时
   - 数据库异常
   - 外部API失败
   - 工具越权调用
   - RAG无结果
   - 上下文超预算
   - Prompt注入输入
   - 紧急症状输入
   - 用户输入为空

4. 部署准备:
   - Dockerfile
   - docker-compose
   - `.env.example`
   - README启动说明
   - 初始化数据库脚本
   - 知识库导入脚本

5. 演示脚本:
   - 注册用户
   - 填写健康档案
   - 导入健康数据
   - 提问健康问题
   - 查看带引用的回答
   - 触发专业Agent
   - 触发安全边界样例
   - 使用Trace ID排查一次请求
   - 运行评估脚本并查看报告

### 14.3 验收标准

- 新电脑按README可以启动项目
- 核心接口测试通过
- 安全类测试通过
- 评估脚本能稳定输出报告
- 前端主流程无明显报错
- 可以完成一段完整项目演示

## 15. Phase 12: 暂缓增强功能

以下功能当前不实现,但设计时应保留合理扩展空间。

### 15.1 多模态图片识别

后续可增加:
- 图片上传接口
- 文件存储
- OCR或多模态模型
- 图片食物识别
- 健康报告图片解析

### 15.2 配置热更新

后续可增加:
- Prompt在线配置
- Agent开关配置
- 工具超时配置
- 检索参数动态配置
- 配置变更审计

### 15.3 系统化版本管理与回归评估

后续可增加:
- Prompt版本号
- 模型版本号
- 检索策略版本号
- 工具逻辑版本号
- 版本间评测对比
- 指标下降阻断上线

### 15.4 复杂服务拆分

后续可在访问量、团队规模或部署复杂度上升后再考虑:
- API服务拆分
- Agent服务拆分
- RAG服务拆分
- 观测与评估服务独立部署

## 16. 新手开发建议

- 每完成一个接口就写一个最小测试,不要等全部写完再测。
- 每天只推进一个主题,例如今天只做用户登录,不要同时改Agent和前端样式。
- 先让主流程跑通,再补齐安全、观测和评估。
- 所有外部API调用都要设置超时和失败兜底。
- 所有LLM输入都要区分系统指令、用户输入、RAG内容和工具结果。
- 所有工具调用都要经过ToolExecutor,不要在Agent节点里直接调用外部API。
- 所有Prompt上下文都要经过ContextAssembler,不要把历史消息和RAG文档直接拼接。
- 健康建议必须保留边界: 不诊断、不替代医生、不提供危险用药指导。
- 每个阶段结束后写一段开发记录,说明完成了什么、还剩什么、遇到什么问题。

## 17. 最终交付定义

当以下能力全部完成时,可以认为项目主线版本开发完成:

- 用户可以注册登录
- 用户可以维护健康档案
- 用户可以导入和查看健康数据
- 用户可以发送文本健康问题
- 系统可以根据用户画像和记忆生成个性化回复
- 健康顾问Agent可以调度营养、运动、环境等专业Agent
- RAG可以返回健康知识引用
- Prompt注入样例不会覆盖系统规则
- 紧急症状会触发就医提示
- 工具调用具备白名单、参数校验、超时和降级
- 上下文组装具备分区Token预算、截断记录和context_trace
- 每次请求都能通过Trace ID追踪
- 日志、指标、Token消耗、tool_trace、context_trace和错误率可查看
- 评估脚本可以输出Agent质量报告
- 前端可以完整演示主要流程
- 后端核心测试、安全测试和评估脚本测试通过
- README可以指导新人启动、初始化和演示项目
