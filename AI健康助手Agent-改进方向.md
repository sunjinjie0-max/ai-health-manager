# AI 健康助手 Agent 改进方向

记录日期：2026-09-02
更新日期：2026-09-04

## 待办清单

| 编号 | 改进项 | 当前问题 | 目标与验收标准 | 优先级 | 难度 |
|---|---|---|---|---|---|
| 1 | 将健康数据摘要传给专业 Agent | 专业 Agent 主要接收用户画像和任务参数，无法充分使用步数、睡眠、心率等健康摘要 | 在 `AgentRequest` 或任务上下文中传递经过裁剪的健康摘要；营养、运动等 Agent 能读取并据此调整结果；补充上下文传递测试 | P0 | 中 |
| 2 | 完善专业 Agent 异常处理与状态判断 | 工具失败会被软化为空数据，Agent 异常会被转换成非空错误文案，`handle()` 因而可能误判为 `success`；缺失值还可能被当作正常值参与建议，全体专业 Agent 失败时调度状态仍可能是 `partial` | 根据关键数据完整性、`tool_trace` 和结构化错误显式判断 `success/partial/failed/timeout/skipped`，禁止用 `response` 是否非空判断；缺失数据不得使用正常默认值，内部异常不得原样返回用户；正确区分部分失败与全部失败，并补充软失败、异常、超时、降级和汇总测试 | P0 | 中 |
| 3 | 接入真实空气质量数据 | 当前空气质量由随机数模拟，数据来源与展示引用不够准确 | 使用 Open-Meteo Air Quality API；增加超时、字段转换和失败降级；正确标记数据来源；补充成功、超时、异常响应测试 | P0 | 中 |
| 4 | 实现真正的前端流式输出 | 后端当前先等待完整 Agent 结果，再把完整回答拆块发送，并非模型 Token 级流式输出 | 打通 LLM 流式响应、后端 SSE 转发和前端增量渲染；支持错误事件、中断和流结束处理 | P1 | 高 |
| 5 | 为扫描版 PDF 增加 OCR | `pypdf` 只能提取已有文字层，图片型 PDF 无法正常入库 | 文本提取为空或过少时触发 OCR；保留页码元数据；补充文本 PDF、扫描 PDF 和 OCR 失败测试 | P1 | 高 |
| 6 | 增加 RAG 相关性阈值 | 当前直接将 Top 5 放入 Prompt，低相关文档也可能影响回答 | 在融合或重排后应用可配置阈值；低于阈值的文档不进入 Prompt；无合格文档时安全降级；补充边界值测试 | P0 | 低 |
| 7 | 更新文件时删除旧 Chunk | 同一知识文件内容变化后会产生新 Chunk ID，旧片段可能继续留在 Elasticsearch | 为源文档建立稳定标识；重新导入前删除或替换该文档旧 Chunk；保证其他文档不受影响；补充重复导入和更新测试 | P1 | 中 |
| 8 | 将短期记忆迁移到 Redis | 当前短期记忆保存在 Python 进程内存，后端重启后丢失，多实例之间无法共享 | 按用户和会话隔离保存最近 N 轮消息；配置 TTL 和清理策略；支持后端重启后继续读取、跨实例共享；缓存缺失或 Redis 故障时从 PostgreSQL 恢复近期聊天上下文；补充重启、共享、隔离和降级测试 | P1 | 中 |
| 9 | 长期记忆冲突处理与时间边界 | 新旧习惯可能同时保留且互相矛盾；ES 检索没有时间过滤，PostgreSQL 降级检索只查最近 180 天；`memory_max_days` 未实际用于检索 | 识别同一用户同类事实的冲突，更新或失效旧记忆并保留来源和历史；按记忆类型制定有效期或时间衰减策略，统一 ES 与数据库降级路径的时间边界；区分查询过滤与实际删除；补充冲突更新、时间边界和降级一致性测试 | P1 | 高 |
| 10 | 启用并验证 Reranker | RAG 已有 Reranker 接入，但默认配置与 Docker Compose 中均关闭，目前使用 RRF 融合后直接取 Top K | 配置重排模型及依赖并开启开关，验证 BM25/向量召回 → RRF → Reranker → Top K 的链路；配合第 6 项相关性阈值，不能仅重排不筛选；评估检索质量和延迟，失败时保留 RRF 排序；补充启停和异常测试 | P1 | 中 |
| 11 | 支持短期与长期记忆同时检索 | 下游检索和上下文拼接支持两种记忆，但当前路由不会同时开启短期与长期记忆，复合问题可能遗漏近期情况或长期习惯 | 扩展路由，使同时依赖近期上下文和历史习惯的问题能够检索两类记忆，必要时再结合 RAG；按预算裁剪和去重，冲突时优先用户当前明确表述；补充短期单独、长期单独、联合检索及 Prompt 组装测试 | P1 | 中 |

## 建议实施顺序

1. 完善专业 Agent 异常处理与状态判断。
2. 增加 RAG 相关性阈值。
3. 将健康数据摘要传给专业 Agent。
4. 接入 Open-Meteo 空气质量 API。
5. 更新文件时清理旧 Chunk。
6. 将短期记忆迁移到 Redis，并完善数据库恢复逻辑。
7. 完善长期记忆冲突处理与时间边界。
8. 启用 Reranker，并与相关性阈值配合验证。
9. 支持短期与长期记忆同时检索和进入上下文。
10. 实现真正的流式输出。
11. 增加 PDF OCR。

## 状态说明

- 清单事项作为后续待实现或待完善的改进记录；本次仅更新文档，不修改业务代码。
- Reranker 已有代码接入，待启用与验证；短期和长期记忆的联合拼接已有支持，待扩展路由并完成链路验证。
- 每完成一项，应同步记录修改文件、测试结果、效果对比和可用于简历的项目描述。
