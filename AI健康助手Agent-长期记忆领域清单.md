# AI健康助手Agent-长期记忆领域清单

## 1. 设计目标

长期记忆不是“把历史对话存起来”，而是把对后续个性化建议真正有价值的用户事实、偏好、约束抽取出来，形成可检索、可去噪、可解释的用户事实库。

长期记忆设计目标：

1. 只沉淀稳定、高价值、跨会话可复用的信息
2. 支持结构化抽取、ES 向量检索和检索后去噪
3. 区分用户事实、阶段性目标、短期上下文和外部知识
4. 为饮食、运动、睡眠、环境、健康风险判断提供长期个性化支撑

---

## 2. 领域清单

| 领域 | 典型字段/槽位 | 示例 | 抽取方式 | 是否建议直接写长期记忆 |
|------|---------------|------|----------|------------------------|
| 基础画像 | `age` `gender` | `28岁` `女性` | 规则优先 | 是 |
| 过敏/不耐受 | `allergy` `intolerance` | `对花生过敏` `乳糖不耐受` | 规则 + LLM补充 | 是 |
| 慢病/健康约束 | `condition` `exercise_constraint` | `高血压` `膝盖不适` | 规则 + LLM补充 | 是 |
| 健康目标 | `health_goal` | `减脂` `控糖` `改善睡眠` | 规则优先 | 是 |
| 运动习惯 | `exercise_time` `exercise_activity` `exercise_frequency` | `周六运动` `跑步` `每周3次` | 规则 + LLM补充 | 是 |
| 饮食偏好 | `diet_preference` | `喜欢燕麦和鸡蛋` | 规则 + LLM补充 | 是 |
| 饮食忌口 | `diet_avoidance` | `不吃香菜` `不吃内脏` | 规则优先 | 是 |
| 饮食约束 | `diet_constraint` | `低盐饮食` `控糖` `素食` | 规则优先 | 是 |
| 饮食模式 | `meal_pattern` | `经常不吃早餐` `夜宵频繁` | 规则 + LLM补充 | 是 |
| 睡眠习惯 | `sleep_time` `sleep_pattern` | `一般12点睡` `周末补觉` | 规则 + LLM补充 | 是 |
| 生活方式约束 | `lifestyle_constraint` | `工作日没时间运动` `久坐` `经常加班` | 规则 + LLM补充 | 是 |
| 情绪/压力状态 | `stress_state` | `最近压力大` `焦虑` | LLM补充 | 谨慎写入 |
| 用药/补剂 | `medication` `supplement` | `在吃降压药` `补充蛋白粉` | 规则 + LLM补充 | 高风险，需保守 |

---

## 3. 长期记忆数据模型

每条长期记忆建议统一成下面的结构：

```json
{
  "id": "sha1(...)",
  "user_id": "user-123",
  "session_id": "session-456",
  "domain": "nutrition",
  "topic": "diet_constraint",
  "memory_type": "constraint",
  "slot": "diet_constraint",
  "value": "低盐",
  "content": "用户饮食约束包含低盐。",
  "confidence": 0.92,
  "salience": 0.88,
  "evidence": "我这段时间需要低盐饮食。",
  "tags": ["nutrition", "diet", "constraint"],
  "created_at": "2026-05-24T10:00:00Z",
  "updated_at": "2026-05-24T10:00:00Z"
}
```

字段说明：

- `domain`：大领域，如 `exercise` `nutrition` `sleep`
- `topic`：更具体的主题，如 `exercise_activity` `diet_preference`
- `memory_type`：事实类型，如 `habit` `constraint` `goal` `condition`
- `slot`：便于回答阶段和检索后重排直接使用
- `value`：规范化后的核心值，如 `跑步` `周六` `低盐`
- `content`：可读的中文事实描述
- `confidence`：抽取置信度
- `salience`：这条记忆对后续个性化的重要程度
- `evidence`：原始证据句
- `tags`：便于后续过滤和归并

---

## 4. 记忆抽取方案

### 4.1 抽取总流程

1. 对用户消息做预清洗  
   去掉无意义空白，保留原始证据句。

2. 规则优先抽取高确定性事实  
   例如：
   - `对X过敏`
   - `不吃X`
   - `喜欢吃X`
   - `习惯周六运动`
   - `运动项目是跑步`
   - `一般12点睡`
   - `经常不吃早餐`

3. 条件触发的 LLM 结构化抽取补充  
   对复杂表达做 JSON 抽取，输出 `slot/value/confidence/evidence`。当前工程实现为：规则优先，只有在规则抽取为空、覆盖不足、多领域混合或表达较隐含时，才额外调用一次 LLM 做补充。

4. 写入门槛控制  
   - 高置信度稳定事实：直接写
   - 中置信度偏好：重复出现 2 次以上再写
   - 低置信度或明显短期表述：只放短期记忆，不进长期记忆

5. 归一化与去噪  
   - `星期六` -> `周六`
   - `慢跑` -> `跑步`
   - `低盐饮食` -> `低盐`
   - 重复事实合并
   - 冲突事实保留更新时间，后写入覆盖旧结论

6. 向量化并写入 ES 记忆索引

---

### 4.2 规则优先抽取策略

#### 运动域

- 时间偏好：
  - `我习惯周六运动`
  - `我一般晚上跑步`
- 项目类型：
  - `我一般进行的锻炼项目是跑步`
  - `我平时喜欢游泳`
- 频率：
  - `我每周运动3次`

#### 饮食域

- 饮食偏好：
  - `我喜欢吃燕麦`
  - `我早餐常吃鸡蛋`
- 饮食忌口：
  - `我不吃香菜`
  - `我不吃内脏`
- 饮食约束：
  - `我在控糖`
  - `我需要低盐饮食`
  - `我吃素`
- 饮食模式：
  - `我经常不吃早餐`
  - `我晚上喜欢吃夜宵`

#### 睡眠域

- 睡眠时间：
  - `我一般12点睡`
  - `我通常7点起`
- 睡眠模式：
  - `我经常熬夜`
  - `我周末会补觉`
  - `我习惯午睡`

#### 生活方式域

- `我工作日没时间运动`
- `我经常加班`
- `我久坐比较多`
- `我通勤时间很长`

---

## 5. 检索与去噪方案

### 5.1 检索

长期记忆查询采用：

1. `user_id` 过滤
2. BGE-M3 向量检索
3. BM25 关键词检索
4. RRF 融合

### 5.2 检索后去噪

检索结果不是原样注入 prompt，而是做三步处理：

1. 去重  
   按 `memory_type + slot + value + content` 去重

2. 重排  
   综合：
   - 检索得分 `score`
   - 显著性 `salience`
   - 置信度 `confidence`
   - 新鲜度 `updated_at`

3. 裁剪  
   只保留 3-6 条最有价值的长期记忆注入 prompt

---

## 6. 当前工程建议的落地顺序

### P1 已实现/优先级最高

- 过敏
- 健康目标
- 慢病
- 运动时间偏好
- 运动项目类型

### P2 本次扩展建议

- 饮食偏好
- 饮食忌口
- 饮食约束
- 饮食模式
- 睡眠习惯
- 生活方式约束

### P3 后续扩展

- 运动频率/训练基础
- 压力与情绪状态
- 用药与补剂
- 阶段性目标变化

---

## 7. 哪些信息不建议直接写长期记忆

- 一次性的短期状态  
  例如：`今天不想吃辣`

- 含义模糊、没有明确主语的表达  
  例如：`最近感觉不太舒服`

- 可能只是当下尝试而非稳定习惯  
  例如：`这周想少吃甜食`

- 高敏感但未经确认的信息  
  例如：精神疾病诊断、怀孕状态、具体处方药

---

## 8. 结论

长期记忆要解决的不是“存更多”，而是“存得准、存得稳、取出来有用”。  
因此工程上的核心不是单纯加检索，而是建立：

`领域清单 -> 结构化抽取 -> 写入门槛 -> 归一化 -> ES 检索 -> 检索后去噪`
