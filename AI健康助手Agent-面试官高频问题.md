# AI健康管家 - 面试官高频问题

---

## 0. 项目一句话定位

AI健康管家是一个面向个人健康管理的多模态、多 Agent 智能健康管理系统。系统以 HealthAdvisor Agent 为主控入口，通过 LangGraph 编排营养、环境、运动等专业 Agent，结合 Elasticsearch + BGE-M3 的 RAG 知识检索、三级记忆系统、用户画像、外部健康工具和医疗安全策略，为用户提供个性化、可解释、可追溯的健康咨询与行动建议。

面试时可以浓缩成这段：

```text
这个项目不是简单调用大模型做问答，而是一个多 Agent 健康管理系统。后端用 FastAPI，Agent 工作流用 LangGraph，主 Agent 负责安全检查、意图识别、任务规划和最终回复，专业 Agent 负责营养、环境、运动等垂直任务。知识增强部分用 Elasticsearch BM25 + BGE-M3 向量混合检索，再接 reranker 和引用溯源；记忆系统包括短期会话记忆、中长期语义记忆和结构化用户画像。健康场景风险比较高，所以我把安全检查、紧急症状升级、免责声明、RAG grounding 和输出审核都放进了 Agent 流程里。
```

---

## 1. 项目介绍类问题

### 1. 请用 1 分钟介绍一下这个项目。

**考察点：项目定位、技术主线、表达是否聚焦 Agent。**

```text
AI健康管家是一个多 Agent 智能健康管理系统，核心目标是把通用健康咨询、饮食营养分析、环境健康提醒、运动规划和长期健康画像整合到一个对话式助手里。

技术上，后端采用 FastAPI，Agent 框架采用 LangGraph，把一次用户咨询拆成加载上下文、安全检查、意图识别、RAG 检索、子 Agent 调度、结果聚合和回复生成等节点。模型层通过 DeepSeek 大模型完成意图识别、信息抽取和自然语言生成。知识增强层使用 Elasticsearch BM25 + BGE-M3 dense vector 混合检索，再通过 reranker 精排，解决健康问答的可靠性和可追溯问题。

系统采用主从式多 Agent 架构，HealthAdvisor Agent 作为主 Agent，营养、环境、运动 Agent 作为专业子 Agent。用户画像和记忆系统会长期积累用户的年龄、健康目标、饮食偏好、过敏史、运动能力等信息，用于个性化建议。健康领域比较敏感，所以我在主流程最前面加入了医疗安全检查，胸痛、呼吸困难、中风、自杀意念等紧急情况会直接提示就医，而不是进入普通问答。
```

### 2. 这个项目解决了什么痛点？

**考察点：产品思维、业务理解。**

```text
我认为它解决了三个痛点。

第一，健康管理信息分散。普通用户的饮食、运动、睡眠、环境、基础健康咨询通常分散在不同 App 或搜索结果里，缺少统一入口。

第二，通用大模型缺少长期用户上下文。比如用户过敏、慢性病、减脂目标、运动基础、饮食偏好这些信息，如果每次都重新描述，体验很差，也很难做到个性化。

第三，健康咨询对可靠性和安全性要求高。普通聊天模型容易幻觉，或者在紧急症状下给出不合适建议。因此这个项目通过专业 Agent 分工、RAG 权威知识增强、用户画像记忆和安全兜底，把健康咨询做得更可控、更可解释。
```

### 3. 这个项目和普通 ChatGPT 套壳有什么区别？

**考察点：是否真正做了 Agent 工程。**

```text
区别主要在四点。

第一，流程不是一次 prompt，而是 LangGraph 状态机。用户消息会依次经过上下文加载、安全检查、意图识别、RAG 检索、子 Agent 调度、结果聚合和后处理。

第二，它不是单 Agent，而是主 Agent + 专业 Agent。HealthAdvisor Agent 负责总控和最终回复，Nutrition Agent、Environment Agent、Exercise Agent 分别处理垂直领域任务。

第三，它有工具和知识系统。营养 Agent 会查营养数据库，环境 Agent 会查天气和空气质量 API，RAG 会检索权威健康知识，回答会带引用和来源。

第四，它有长期用户记忆。系统会维护用户画像和历史健康记忆，比如年龄、目标、过敏、疾病史、运动偏好，用于生成个性化建议。
```

### 4. 如果简历只能写 3 个项目亮点，你会怎么写？

**考察点：简历表达和技术亮点提炼。**

```text
1. 设计并实现基于 LangGraph 的多 Agent 健康咨询工作流，将安全检查、意图识别、RAG 检索、任务规划、专业 Agent 调度和结果聚合拆成可观测节点。

2. 构建 Elasticsearch BM25 + BGE-M3 dense vector 的健康知识 RAG 系统，支持关键词与语义混合召回、RRF 融合、reranker 精排和引用溯源，降低健康问答幻觉。

3. 设计用户画像与三级记忆系统，结合 Redis 短期会话、PostgreSQL 结构化画像和 ES 向量长期记忆，实现长期个性化健康建议。
```

### 5. 项目里你主要负责什么？

**考察点：个人贡献是否具体。**

```text
我主要负责后端 Agent 架构和核心链路。具体包括 BaseAgent 抽象、AgentRegistry 注册机制、AgentOrchestrator 编排器、HealthAdvisor 主 Agent 工作流，以及 Nutrition、Environment、Exercise 三个专业 Agent 的状态设计和节点实现。

另外我也负责 RAG 和记忆系统设计。RAG 部分包括 ES 索引结构、BGE-M3 embedding、BM25 + vector 混合召回、RRF 融合、reranker 精排和引用拼接。记忆系统包括短期会话记忆、中长期语义记忆和用户画像合并策略。

工程化部分，我参与了 FastAPI 接口设计、JWT 鉴权、Redis 缓存、Celery 异步任务、日志和测试设计。
```

---

## 2. 整体架构类问题

### 6. 整体系统架构是怎样的？

**考察点：分层设计能力。**

```text
整体分为六层。

第一层是接入层，主要是 H5/Vue 前端和 FastAPI API Gateway，提供聊天、图片上传、专业 Agent 调用、用户画像、健康档案等接口。

第二层是 Agent 编排层，核心是 AgentOrchestrator，负责意图识别、任务拆解、依赖分析、并行/串行调度、超时控制和结果聚合。

第三层是专业 Agent 层，包括 HealthAdvisor、Nutrition、Environment、Exercise 等 Agent，每个 Agent 都基于 LangGraph 定义自己的状态机。

第四层是能力层，包括 LLM Provider、RAG Retriever、Memory System、Tool Registry、多模态理解和安全策略。

第五层是工具与外部服务层，包括 OCR、营养数据库、天气 API、空气质量 API、健康数据导入工具、通知工具等。

第六层是存储层，包括 PostgreSQL 存用户、画像、健康档案和会话元数据，Redis 存短期会话和热点缓存，Elasticsearch 存知识库与向量记忆，MinIO/OSS 存图片和文件。
```

### 7. 为什么选择 FastAPI？

**考察点：Web 框架选型。**

```text
FastAPI 比较适合这个项目，因为 Agent 系统里有大量 IO 密集操作，比如 LLM 调用、RAG 检索、数据库查询、Redis 缓存、外部 API 调用。FastAPI 原生支持 async，可以比较自然地把这些调用异步化。

另外 FastAPI 基于 Pydantic，接口入参和响应结构可以强校验，对 Agent 间通信、专业 Agent 结构化输出也比较友好。它自动生成 Swagger 文档，方便前后端联调和面试展示。Python AI 生态也比较完整，和 LangChain、LangGraph、SQLAlchemy、Redis、Celery 集成成本低。
```

### 8. 为什么选择 LangGraph，而不是只用 LangChain Agent？

**考察点：Agent 框架理解。**

```text
LangChain Agent 更适合工具调用循环，比如 ReAct 或 Function Calling；但这个项目的核心不是让模型自由选择工具，而是要把健康咨询流程做成可控状态机。

健康咨询有强流程约束，例如必须先做安全检查，紧急症状必须直接进入就医提示，不能让模型自由决定是否跳过。再比如多 Agent 调度需要明确任务依赖，环境 Agent 的结果可能要注入运动 Agent，这种流程用图结构表达更清晰。

LangGraph 的优势是节点、边、条件路由和状态都显式可见，便于调试、观测、测试和恢复。对生产级 Agent 系统来说，可控性比“模型自由循环”更重要。
```

### 9. 为什么要做 Multi-Agent，而不是一个大 Prompt？

**考察点：架构权衡。**

```text
我选择 Multi-Agent 主要是因为健康管理本身是多领域问题。营养、运动、环境、通用健康咨询的知识、工具和输出结构都不一样，如果放在一个大 Prompt 里，会越来越难维护，也容易互相干扰。

拆成多个专业 Agent 后，每个 Agent 可以有自己的状态字段、工具、知识库和评估标准。营养 Agent 关注食物抽取和营养计算，环境 Agent 关注 AQI 和天气风险，运动 Agent 关注体能评估和安全约束。

此外 Multi-Agent 更利于扩展。后续新增 SleepAgent、MedicationAgent、ReportAnalysisAgent，只需要接入注册中心和路由策略，不必重写主 Agent。
```

### 10. Multi-Agent 的代价是什么？

**考察点：是否理解架构成本。**

```text
Multi-Agent 的代价主要是调度复杂度和一致性问题。

调度复杂度方面，需要处理任务拆解、依赖关系、并发执行、超时、重试和降级。例如运动 Agent 可能依赖环境 Agent 的空气质量结果，不能简单并行。

一致性方面，不同 Agent 可能给出冲突建议，比如运动 Agent 建议户外跑步，但环境 Agent 判断 AQI 较差。这个时候需要主 Agent 做结果聚合和冲突仲裁。

成本方面，多 Agent 可能带来多次 LLM 调用，所以需要缓存、规则前置、小模型分类和并发控制来降低成本。
```

### 11. AgentOrchestrator 具体负责什么？

**考察点：编排层设计。**

```text
AgentOrchestrator 是多 Agent 系统的流程控制中心。它主要负责五件事。

第一是任务规划，根据主 Agent 的意图识别结果，把用户问题拆成一个或多个子任务。

第二是依赖分析，比如环境结果可以作为运动计划的输入，营养和环境任务则可以并行执行。

第三是调度执行，对无依赖任务并发执行，对有依赖任务按 DAG 分批执行。

第四是异常处理，包括单 Agent 超时、失败重试、外部 API 不可用、依赖缺失时的降级。

第五是结果聚合，把多个子 Agent 的结构化结果交给 HealthAdvisor Agent，生成统一自然语言回复。
```

### 12. Agent 间通信协议怎么设计？

**考察点：系统工程能力。**

```text
我会把 Agent 间通信设计成标准结构化协议，而不是直接传自然语言。

请求协议包含 trace_id、user_id、session_id、target_agent、task_type、payload、user_profile、prior_results、deadline 等字段。

响应协议包含 agent、status、result、confidence、citations、warnings、metadata、error 等字段。

这样做的好处是主 Agent 可以稳定聚合结果，也便于日志追踪和评测。比如环境 Agent 返回的不是一句“今天空气不好”，而是 AQI、PM2.5、risk_level、sensitive_groups、recommendations 等结构化字段，运动 Agent 可以直接使用这些字段调整运动计划。
```

### 13. 如果用户一个问题涉及多个领域，怎么调度？

**考察点：任务拆解与依赖调度。**

```text
以“我今天吃了宫保鸡丁，北京空气质量怎么样，适合跑步减脂吗”为例。

主 Agent 首先识别这是营养、环境、运动三个领域的 multi_domain 问题。编排器会拆成三个子任务：饮食分析、环境查询、运动计划。

营养分析和环境查询没有依赖，可以并行执行。运动计划依赖环境结果，因为是否适合户外跑步要看 AQI、温度、湿度和紫外线，所以运动 Agent 会在环境 Agent 完成后执行，并把环境结果作为 prior_results 注入。

最后 HealthAdvisor Agent 聚合三个结果，给出饮食评价、环境风险和运动建议。如果空气质量差，最终回复会建议改为室内有氧或低强度训练。
```

---

## 3. HealthAdvisor Agent 与安全流程

### 14. HealthAdvisor Agent 的工作流是什么？

**考察点：主 Agent 状态机。**

```text
HealthAdvisor Agent 是默认对话入口，它的 LangGraph 流程大致是：

load_context -> check_safety -> classify_intent -> memory_route -> retrieve_memory -> plan_tasks -> dispatch_agents 或 rag_retrieve -> generate_response -> post_process。

load_context 会加载用户画像、短期会话历史和健康档案摘要。

memory_route 会先判断当前问题是短期追问、长期事实回忆，还是通用知识问答。

retrieve_memory 只在需要时检索长期记忆，长期记忆本身存储在 ES 向量索引里，按 user_id 过滤后做向量召回。

check_safety 会识别胸痛、呼吸困难、中风、自杀意念等紧急情况。如果命中高风险，会直接进入 urgent_reply，不再走普通问答。

classify_intent 会识别 nutrition、environment、exercise、general_health、multi_domain 等意图。

plan_tasks 和 dispatch_agents 负责专业 Agent 调度。

generate_response 会结合用户画像、RAG 知识、子 Agent 结果和安全规则生成最终回复。

post_process 负责生成追问、写入记忆、异步更新画像和记录 trace。
```

### 15. 为什么安全检查要放在最前面？

**考察点：医疗安全意识。**

```text
健康场景和普通问答不同，安全优先级最高。如果用户描述胸痛、呼吸困难、意识模糊、大出血、自杀意念等情况，系统不能先去做 RAG 或调用专业 Agent，更不能让模型自由生成普通建议。

所以我把 safety check 放在整个工作流最前面。一旦判定为高风险，直接返回固定就医模板，比如“立即拨打 120 或前往急诊”，并避免提供药物剂量、治疗方案或居家处理建议。

这是为了降低延误就医和医疗误导风险。
```

### 16. 安全检查怎么实现？

**考察点：规则和模型结合。**

```text
安全检查分两层。

第一层是规则关键词，覆盖胸痛、呼吸困难、昏迷、大出血、中风、自杀意念等高风险词。这一层速度快、可解释，适合做硬兜底。

第二层是 LLM 安全分类。对于没有明显关键词但语义上可能有风险的问题，用模型输出结构化 JSON，包括 is_urgent、risk_level、warning_message、recommendations。

最终路由策略是安全优先。只要判断为 high risk，就直接进入 urgent_reply。对于中低风险，会在正常回答中加入就医建议或免责声明。
```

### 17. 为什么紧急回复不用大模型自由生成？

**考察点：高风险场景控制。**

```text
紧急医疗场景不适合让大模型自由发挥，因为模型可能输出看似合理但不安全的处理建议。比如胸痛时，如果模型建议“先休息观察”，可能造成严重后果。

所以 urgent_reply 使用固定模板，保证核心信息稳定：立即拨打急救电话、前往急诊、不要等待在线回复、找身边人协助。模板化可以牺牲一点灵活性，但换来更高的安全性和一致性。
```

### 18. 意图识别怎么做？

**考察点：路由策略。**

```text
意图识别采用规则 + LLM 结构化分类结合。

简单高置信场景可以先用关键词或轻量分类器快速识别，例如“热量、蛋白质、吃了什么”偏 nutrition，“AQI、天气、雾霾”偏 environment，“跑步、减脂、训练计划”偏 exercise。

复杂场景交给 LLM 输出 JSON，字段包括 intent、confidence、urgency、sub_tasks、requires_medical_attention。对于 multi_domain 问题，LLM 会输出多个子任务，交给 Orchestrator 做 DAG 调度。

这样既能保证低延迟，又能处理复杂自然语言。
```

### 19. 意图识别错了怎么办？

**考察点：路由容错、低置信度处理、Agent 纠错机制。**

```text
意图识别一定会有错分，所以不能把它设计成一次性、不可修正的硬路由。我会从“识别前、识别中、识别后”三层做容错。

第一，识别阶段要输出 confidence，而不是只输出 intent。高置信度问题可以直接路由；低置信度问题进入澄清或多 Agent 兜底。例如用户说“最近状态不好，吃也吃不好，运动也没动力”，它可能同时涉及营养、运动、睡眠或心理状态，这种就不应该强行只分到一个 Agent。

第二，对多领域问题允许输出多个 sub_tasks。比如“结合杭州天气，帮我安排减脂晚餐和跑步计划”，就应该同时调度 EnvironmentAgent、NutritionAgent 和 ExerciseAgent，而不是单一路由。

第三，子 Agent 执行后要允许二次纠偏。如果 NutritionAgent 发现用户问题其实主要在问训练计划，可以返回 low_confidence 或 suggested_route=exercise，主 Agent 再重新调度。

第四，最终回答阶段做结果一致性检查。如果路由到运动 Agent，但回答里没有覆盖用户真正问的“饮食怎么吃”，就说明路由或任务拆解有问题，可以追加调用 NutritionAgent 或在回复中主动补齐。

第五，评估集中要专门放易混淆样本，比如“增肌吃什么”“跑步前吃什么”“空气差还能运动吗”“睡不好还适合训练吗”。通过这些 case 评估 intent accuracy、multi_intent recall 和 route hit rate。

企业落地时，我不会追求一次路由永远正确，而是让系统具备低置信度澄清、多 Agent 并行兜底、执行后纠偏和失败 case 回流能力。
```

### 20. HealthAdvisor 如何聚合子 Agent 结果？

**考察点：结果综合能力。**

```text
子 Agent 返回的是结构化结果，不是纯文本。比如 Nutrition Agent 返回营养总览、健康评分、建议；Environment Agent 返回 AQI、天气、健康风险；Exercise Agent 返回训练计划和安全提示。

HealthAdvisor 会先做冲突检测。例如运动 Agent 推荐户外跑，但环境 Agent 判断空气质量差，主 Agent 会按安全优先原则改成室内运动建议。

然后主 Agent 会结合用户画像和 RAG 知识生成一段统一回复，避免把多个 Agent 的结果简单拼接。最终回复会尽量体现“为什么这样建议”，并保留引用和免责声明。
```

---

## 4. 专业 Agent 问题

### 21. Nutrition Agent 的流程是什么？

**考察点：专业 Agent 设计。**

```text
Nutrition Agent 的流程是 parse_input -> extract_food -> normalize_food -> query_nutrition -> analyze_nutrition -> generate_recommendations -> format_response。

parse_input 负责判断输入是文字、图片还是混合输入。

extract_food 用 LLM 或视觉模型从用户描述或图片中抽取食物、数量、单位和置信度。

normalize_food 会把“半碗米饭”“一杯奶茶”转换成标准食物和克重。

query_nutrition 会查询营养数据库，比如中国食物成分表或第三方营养 API。

analyze_nutrition 计算热量、蛋白质、脂肪、碳水、膳食纤维、钠等指标，并结合用户目标和健康限制评分。

generate_recommendations 给出个性化建议，例如减脂用户控制热量，高血压用户关注钠摄入，过敏用户避开风险食材。
```

### 22. Nutrition Agent 如何处理图片？

**考察点：多模态能力。**

```text
图片输入会先进入多模态理解模块。

如果是食品包装、营养成分表或外卖小票，可以走 OCR，把文字识别出来，再交给 Nutrition Agent 抽取营养成分。

如果是餐盘图片，可以用视觉模型识别食物类别和大致份量。识别结果会带置信度，低置信度时会向用户追问，比如“这份肉大概是鸡肉还是猪肉，份量约多少？”

识别后的结构化食物列表会复用文字营养分析流程，也就是标准化、查库、计算和建议。
```

### 23. Environment Agent 的流程是什么？

**考察点：外部工具型 Agent。**

```text
Environment Agent 的流程是 resolve_location -> fetch_air_quality -> fetch_weather -> assess_risk -> generate_advice -> format_response。

resolve_location 负责从用户消息或用户画像中确定城市和坐标。

fetch_air_quality 调用空气质量 API，获取 AQI、PM2.5、PM10、O3、NO2、SO2、CO 等指标。

fetch_weather 调用天气 API，获取温度、湿度、风速、紫外线、降水概率等。

assess_risk 根据 AQI、温度、湿度、紫外线等规则评估健康风险，识别儿童、老人、呼吸系统疾病、心血管疾病等敏感人群。

generate_advice 给出户外活动、运动、防晒、口罩、室内通风或空气净化建议。
```

### 24. Exercise Agent 如何生成运动计划？

**考察点：个性化推荐。**

```text
Exercise Agent 会综合用户目标、体能水平、健康限制、可用时间、运动场景和环境数据。

首先它会识别用户目标，比如减脂、增肌、心肺提升、柔韧性或健康维持。然后判断体能水平，比如 beginner、intermediate、advanced。

接着会读取用户画像中的慢性病、伤病、年龄、运动偏好。如果用户有高血压、糖尿病、膝关节问题，计划会降低强度或避开高冲击动作。

如果有环境 Agent 的结果，运动 Agent 会判断是否适合户外运动。空气质量差时，会推荐室内低冲击有氧；高温或强紫外线时，会调整运动时间和补水建议。
```

### 25. 三个专业 Agent 如何协同？

**考察点：多 Agent 协作。**

```text
协同主要通过 Orchestrator 和 prior_results 完成。

Nutrition 和 Environment 通常可以并行，因为它们没有直接依赖。Exercise 可能依赖 Environment，因为运动计划需要考虑天气和空气质量。

例如用户问“今天北京空气质量适合跑步吗，我还想减脂，晚饭怎么吃？”系统会并行调用 Environment 和 Nutrition，Environment 结果再注入 Exercise，最后 HealthAdvisor 统一给出晚饭建议和运动方案。

这种方式比单 Agent 更清晰，因为每个 Agent 只负责自己领域的判断，主 Agent 负责综合。
```

---

## 5. RAG 技术选型与实现

### 26. RAG 为什么选 Elasticsearch + BGE-M3？

**考察点：RAG 选型。**

```text
健康问答有两类检索需求。

第一类是精确匹配，比如 BMI、AQI、空腹血糖、高血压阈值，这些依赖关键词、数字和术语，BM25 很合适。

第二类是语义泛化，比如用户说“血压有点高，吃饭要注意什么”，知识库可能写的是“高血压膳食干预”。这种场景需要 embedding 语义检索。

所以我选择 Elasticsearch + BGE-M3。ES 负责全文检索、向量检索、过滤和工程化索引管理；BGE-M3 负责中文语义向量。两者结合可以同时覆盖精确召回和语义召回。
```

### 27. RAG 的完整流程是什么？

**考察点：检索增强生成全链路。**

```text
完整流程是 query preprocessing -> query embedding -> hybrid retrieval -> fusion -> rerank -> context assembly -> answer generation -> citation verification。

用户问题先做清洗和必要的 query rewrite，比如把口语问题补充成检索友好的表达。

然后用 BGE-M3 生成 query embedding。

检索时同时走两路：一路是 ES BM25，对 content、title、topic、category 做关键词召回；另一路是 ES dense_vector kNN，用 embedding 做语义召回。

两路结果用加权 RRF 融合，兼顾关键词和语义。然后可选用 BGE reranker 对候选文档精排。

最后把 top-k 文档按 token budget 拼入 prompt，让 LLM 基于引用资料回答，并返回 citations。
```

### 28. RAG 召回的时候 Top K 的 K 怎么确定？

**考察点：RAG 参数调优、召回质量、成本和延迟权衡。**

```text
Top K 不是拍脑袋定的，也不是越大越好。K 的本质是在召回充分性、上下文噪声、token 成本和延迟之间做平衡。

我一般会分两层确定 K。第一层是初召回 K，比如 BM25 取 top 20、向量检索取 top 20，目标是尽量保证相关文档不要漏掉。第二层是最终注入 Prompt 的 K，比如 rerank 后只保留 top 3 到 top 5，目标是控制上下文质量和 token 长度。

离线调参时会用评测集做对比，看 recall@k、MRR、nDCG、context precision、faithfulness 和 citation correctness。比如 K 从 3 增加到 5，证据命中率可能明显提升；但从 5 增加到 10，召回提升很小，噪声和 token 成本反而增加，那最终注入就不应该取 10。

不同问题类型 K 也可以不同。事实型问题，比如“成年人每周运动多久”，通常 top 3 就够；综合建议类问题，比如“高血压用户如何安排饮食和运动”，可能需要 top 5，因为它涉及饮食、运动、安全边界多个方面；高风险医疗问题则不能单纯依赖 RAG，要先走安全策略。

线上还可以做动态 Top K。先取较大的候选集，然后根据相关性分数、reranker 分数、metadata topic、来源可信度和 token budget 自适应截断。如果 top 1 和 top 2 分数明显高于后面，就少注入；如果问题复杂且多个 chunk 都相关，就多注入几条。

所以我的策略是：初召回 K 稍大，保证 recall；最终注入 K 较小，保证 precision。具体数值通过评测集和线上日志持续调，而不是固定一个永远不变的 K。
```

### 29. 什么是 RRF？为什么用它融合结果？

**考察点：混合检索排序。**

```text
RRF 是 Reciprocal Rank Fusion，它不直接依赖不同检索器的原始分数，而是根据排名做融合。公式大致是 score += weight / (k + rank)。

我用 RRF 是因为 BM25 分数和向量相似度分数不在同一个尺度上，直接相加不稳定。RRF 用排名融合，更鲁棒。

在这个项目里，我给向量检索和 BM25 设置不同权重。健康问答中语义理解很重要，所以向量权重会略高；但医学指标和数值又很依赖关键词，所以 BM25 也保留较高权重。
```

### 30. 为什么还需要 reranker？

**考察点：召回和排序差异。**

```text
BM25 和向量检索属于初召回，目标是尽量不要漏掉相关文档；reranker 是精排，目标是把最能回答当前问题的文档排到前面。

向量召回有时会召回语义相近但不真正回答问题的内容，BM25 也可能因为关键词重合召回噪声。reranker 用 cross-encoder 的方式同时看 query 和 document，可以更准确判断相关性。

所以生产级 RAG 一般是先多路召回，再用 reranker 精排 top-k。
```

### 31. 如何设计知识库文档结构？

**考察点：知识工程。**

```text
每个 chunk 至少包含 id、title、content、source、metadata、updated_at、embedding。

metadata 里会放 category、topic、适用人群、来源等级、发布时间、版本号等信息。健康知识很重视来源和时效，所以 source 和 updated_at 必须保留。

切分策略上，不能按固定字符粗暴切。更合理的是按章节、语义段落和知识点切 chunk，并保留标题层级。比如高血压指南可以按诊断标准、饮食建议、运动建议、用药注意事项拆分。
```

### 32. 如何降低 RAG 幻觉？

**考察点：可靠性。**

```text
我主要从五个方面降低幻觉。

第一，检索使用权威知识库，不把模型记忆当事实来源。

第二，prompt 明确要求基于检索资料回答，资料不足时说明不确定。

第三，回答带 citations，用户和系统可以追溯来源。

第四，对高风险医疗问题先走 safety check，不让普通 RAG 回答覆盖紧急就医建议。

第五，做答案一致性检查，比如回答中的关键数字和建议必须能在引用资料中找到依据。
```

### 33. 如何评估 RAG 效果？

**考察点：评估体系。**

```text
检索层看 recall@k、MRR、nDCG、命中文档来源质量。

生成层看 faithfulness、answer relevance、citation correctness 和是否回答了用户问题。

安全层看紧急症状是否触发升级、是否避免诊断和处方、是否有免责声明。

工程层看检索延迟、ES 查询耗时、embedding 耗时、reranker 耗时、缓存命中率和失败率。

我会构建一套健康问答 golden set，覆盖饮食、运动、环境、慢病、紧急症状和拒答边界。
```

---

## 6. 记忆系统与用户画像

### 34. 记忆系统怎么设计？

**考察点：Agent memory。**

```text
我把记忆分成短期记忆、中期记忆和长期记忆。

短期记忆存最近多轮对话，主要用于当前会话上下文，一般放 Redis，按 session_id 设置 TTL。

中期记忆存最近一段时间的重要健康事件和对话摘要，例如最近 30 天的饮食、运动、睡眠问题，可以放 PostgreSQL 和 ES。

长期记忆存用户稳定画像和长期偏好，例如年龄、性别、过敏史、慢性病、健康目标、饮食偏好、运动能力等。结构化画像放 PostgreSQL，语义记忆放 ES 向量索引。

另外我会在记忆系统前面加一层 memory routing，而不是每次都把短期记忆、长期记忆和知识库全部塞给模型。先判断这轮问题是“追问刚才说过的话”“回忆跨会话长期习惯”，还是“查询通用健康知识”，再决定只读短期记忆、检索长期记忆，还是走 RAG。
```

### 补充题：记忆路由怎么设计？怎么判断只需要短期记忆，而不是检索长期记忆？

**考察点：Memory routing、上下文裁剪。**

```text
我的做法是把记忆访问拆成两步：先路由，再检索。

第一步是 memory routing。系统先判断当前问题依赖的是哪类上下文：短期会话记忆、长期用户记忆，还是外部知识库。比如“我刚才说过什么”“你前面提到的运动习惯”这类问题，通常只需要当前 session 的短期记忆；“我平时一般什么时候运动”“我以前是不是说过对花生过敏”这类问题，更适合检索跨会话的长期记忆；“建议多大的身体活动量”这种是通用知识问题，直接走 RAG 就够了。

第二步才是按策略去取数据。如果路由结果是 short_only，就只加载最近几轮对话，不查长期记忆，也不一定查 RAG；如果是 long_and_rag，就先检索用户历史习惯或健康约束，再补充知识库里的指南和标准。

这样做的好处是三个：一是减少无关上下文，降低 prompt 噪声；二是减少不必要的数据库和向量检索，降低延迟和成本；三是让“用户事实”和“外部知识”的边界更清晰，回答更稳定、更容易解释。
```

### 35. 记忆抽取是如何做的？怎么挖掘出那些历史信息有价值的东西？

**考察点：Memory extraction、长期记忆沉淀。**

```text
我不会把整段历史对话原样塞进长期记忆，而是先做一层记忆抽取，把真正稳定、可复用、对后续决策有帮助的信息提炼出来。

抽取时我主要看三类信息。第一类是稳定事实，比如年龄、过敏史、慢性病、长期健康目标；第二类是长期偏好和习惯，比如“习惯周六跑步”“晚上锻炼”“不吃辣”；第三类是长期约束，比如膝盖不舒服、不适合高冲击运动。

实现上通常是规则优先，再配合结构化抽取。像“对花生过敏”“习惯周六运动”“最近在减脂”这类高价值信息，可以用规则和正则先高精度提取；更复杂的句子再交给 LLM 输出 memory_type、topic、content、salience、evidence 这类结构化字段。

不是所有历史都值得记。像寒暄、一次性的闲聊、没有明确主语的泛泛表达、和后续个性化建议无关的信息，我都不会进入长期记忆。这样做的目的，是让长期记忆更像“用户事实库”，而不是“聊天流水账”。
```

### 36. 检索到的信息有没有做去噪？

**考察点：检索后处理、信息质量控制。**

```text
有，我会把“检索”和“去噪”分开看。很多时候真正影响回答质量的，不是能不能召回，而是召回之后有没有把噪声压下去。

去噪主要做两层。第一层是入库前去噪，只把有价值的长期事实、偏好和约束写进记忆索引，不把整段聊天原样入库。这样可以从源头减少噪声。

第二层是检索后去噪。召回出来的记忆结果会做去重、按 memory_type/topic/content 合并，再结合向量分数、BM25 分数和 salience 重新排序，只保留少量最有价值的记忆注入 prompt。

如果有多条表达相近的记忆，例如“周六跑步”“周六锻炼”“周末运动”，还可以再做一层归并或摘要，最后只保留一条更稳定的用户习惯。这样 prompt 更干净，回答也更稳定。
```

### 37. 陪伴型 Agent 记忆越来越多后，出现冲突怎么办？单靠 RAG 能解决吗？

**考察点：Memory governance、记忆冲突消解、长期陪伴一致性。**

```text
单靠 RAG 很难彻底解决记忆冲突。因为 RAG 本质上解决的是“从一堆内容里召回相关信息”，它不能天然判断哪条记忆更新、哪条更可信、哪条已经失效。陪伴型 Agent 的记忆会长期积累，用户偏好和状态也会变化，比如用户以前说“喜欢晚上跑步”，后来又说“最近改成周六上午跑步”；如果只是向量检索，两条记忆可能都会被召回，模型就会不知道该相信哪一条。

所以我会把记忆系统设计成“检索 + 治理”的模式，而不是只做向量召回。

第一，记忆入库时要结构化，不只存 content，还要存 memory_type、domain、confidence、importance、source_turn、created_at、updated_at、valid_from、valid_until、status 等字段。这样后续才能做版本和有效性判断。

第二，对同一类记忆建立冲突检测机制。比如“运动时间偏好”这个 slot 下，已有记忆是“晚上跑步”，新抽取到“周六上午跑步”，系统会判断它们是否是同一字段的不同取值。如果是互斥信息，就不会简单追加，而是进入覆盖、合并或待确认流程。

第三，设置优先级策略。一般来说，结构化画像优先于普通聊天记忆；用户明确纠正优先于模型推断；最近高置信度记忆优先于很久以前的低置信度记忆；健康安全约束优先于普通偏好。例如“花生过敏”这类安全约束不能因为一次模糊表达就被覆盖。

第四，引入时间衰减和状态管理。长期偏好不是永久不变的，可以把记忆分成 active、superseded、expired、uncertain。旧记忆不一定物理删除，但会被标记为 superseded，默认不注入 prompt，只在追溯历史时使用。

第五，对高风险或低置信度冲突让用户确认。比如用户之前说“不吃海鲜”，后来又说“周末吃了虾”，系统不能直接把“不吃海鲜”删除，而是可以问：“你之前提到不吃海鲜，这次是偶尔吃虾，还是现在饮食偏好发生变化了？”确认后再更新画像。

最终生成回答时，也不是把所有相关记忆都塞给模型，而是先经过 memory resolver 产出一份“当前可信用户状态”，再注入 Prompt。这样模型看到的是整理后的事实，而不是互相矛盾的记忆列表。
```

可以补充一个例子：

```text
比如用户一开始说“我习惯晚上跑步”，后来又说“最近改成周六上午跑步”。普通 RAG 可能把两条都召回，回答就会摇摆。我的处理方式是把它们识别为同一个 domain=exercise、slot=preferred_time 的冲突记忆，新记忆因为时间更新、表达明确，就把旧记忆标记为 superseded；如果表达不够明确，就先保留旧记忆，并向用户确认。这样陪伴型 Agent 才能随着用户变化而更新，而不是越记越乱。
```

### 38. 企业级落地时，记忆生命周期怎么设计？

**考察点：Memory lifecycle、数据治理、长期维护。**

```text
企业级落地时，记忆不能只考虑“怎么写进去”，更重要的是生命周期管理。否则记忆会越积越多，最后变成不可控的数据负担。

我会把记忆生命周期分成六个阶段：采集、抽取、确认、存储、使用、过期或删除。

第一阶段是采集，不是所有对话都进入长期记忆。系统只从用户明确表达的事实、偏好、约束、目标和重要事件中抽取记忆，比如过敏史、慢病、运动习惯、饮食偏好、健康目标。

第二阶段是抽取和结构化。每条记忆要有 domain、memory_type、slot、value、confidence、importance、evidence、source_turn、created_at、updated_at、status 等字段，不能只存一段自然语言。

第三阶段是确认。对于高风险或低置信度记忆，比如疾病史、过敏、用药、妊娠、严重症状，不能直接写成强事实，可以先标记为 uncertain，必要时让用户确认。

第四阶段是存储。稳定结构化画像放 PostgreSQL，非结构化偏好和历史事件放 ES 向量记忆，短期上下文放 Redis。这样可以兼顾事务一致性、语义检索和低延迟读取。

第五阶段是使用。不是每次都取全部记忆，而是通过 memory routing 判断是否需要短期、长期或画像，再经过 memory resolver 汇总成当前可信状态。

第六阶段是过期和删除。记忆要支持 TTL、时间衰减、superseded 状态、用户主动删除和合规删除。旧偏好可以过期，安全相关约束要更谨慎处理。
```

### 39. 记忆系统上线后怎么评估效果？

**考察点：Memory evaluation、A/B 实验、线上指标。**

```text
记忆系统不能只靠人工体验判断，需要设计专门的评估集和线上指标。我会从“记得准、用得对、不乱用、可解释”四个维度评估。

离线评估方面，可以准备跨会话多轮 case。比如第一轮用户说“我周六习惯跑步”，新会话问“我一般什么时候运动”；期望系统能检索到长期记忆，并回答周六。类似地，还可以覆盖饮食偏好、过敏、慢病、运动限制、健康目标等场景。

核心指标包括 memory extraction precision/recall、memory retrieval hit rate、memory grounding accuracy、conflict resolution accuracy 和 personalization pass rate。

线上评估方面，可以看用户纠错率和确认率。比如用户经常说“我不是这个意思”“我没有这样说过”，说明记忆误用率偏高；如果用户经常认可系统提到的历史偏好，说明记忆命中和使用比较好。

另外还要看负向指标，比如无关记忆注入率、过期记忆使用率、隐私字段误注入率、记忆检索延迟和 token 增量。企业落地时，记忆不是越多越好，而是要能稳定提升个性化，同时不引入明显风险。
```

可以补充一组指标：

```text
我会把记忆评估拆成几类指标：

1. 抽取准确率：该记的是否记住，不该记的是否过滤。
2. 检索命中率：用户追问历史偏好时，能否召回正确记忆。
3. 使用准确率：召回后是否真的被用于回答。
4. 冲突消解准确率：新旧偏好冲突时是否选择正确版本。
5. 安全合规指标：敏感记忆是否最小化注入，删除后是否不再使用。
```

### 40. 记忆系统如何做可观测性和排障？

**考察点：Memory observability、Trace、线上排障。**

```text
记忆相关问题很难排查，因为用户只看到最终回答，但中间可能出错的地方很多：可能是没抽取、没写入、没召回、召回了但没注入、注入了但模型没用，或者用了过期记忆。

所以我会给记忆链路做完整 Trace。一次对话里记录 memory routing 结果、抽取出的 candidate memories、写入结果、检索 query、召回的 memory_id、分数、过滤原因、最终注入 Prompt 的记忆摘要，以及生成回答中实际引用了哪些用户记忆。

排障时可以根据链路定位问题。如果用户说“我刚说过周六运动，你为什么不知道”，就看短期记忆有没有写入；如果是跨会话没记住，就看长期记忆有没有抽取和入库；如果入库了但没召回，就看 embedding、domain filter、top_k 和 rerank；如果召回了但没回答，就看 Prompt 注入位置和生成约束。

企业里还要做记忆质量 dashboard，例如每日新增记忆数、抽取成功率、长期记忆召回率、冲突记忆数量、用户纠错反馈、记忆相关失败 case 等。这样记忆系统才能持续运营，而不是只靠人工感觉。
```

### 41. 记忆系统如何处理隐私、用户删除和合规要求？

**考察点：隐私合规、数据可删除、最小化原则。**

```text
陪伴型 Agent 的记忆涉及大量个人信息，健康场景还包含敏感数据，所以必须按“最小化采集、可解释使用、可撤回、可删除”来设计。

首先，记忆采集要分级。普通偏好可以自动记忆，但过敏、疾病史、用药、心理状态这类敏感信息要提高置信度门槛，必要时让用户确认。

其次，Prompt 注入要最小化。即使系统存了完整画像，也不能每次都把全部信息发给模型，只注入当前任务必要字段。例如用户问天气跑步计划，只需要运动习惯、城市、运动限制，不需要完整饮食偏好或历史体检信息。

第三，用户要能查看、编辑和删除记忆。删除不能只删 PostgreSQL，还要同步删除 ES 向量记忆、Redis 缓存、摘要缓存和可能的搜索索引。对于已经进入日志的数据，要做脱敏或按日志生命周期处理。

第四，记忆要有审计信息。每条记忆需要知道来源于哪轮对话、什么时候写入、由什么规则或模型抽取、是否经过用户确认。这样出现争议时可以追溯。

最后，敏感记忆默认不用于营销、推荐或无关任务，只用于用户当前授权的健康建议场景。
```

### 42. 记忆很多时，如何控制成本、延迟和 Prompt 长度？

**考察点：性能优化、上下文压缩、成本控制。**

```text
记忆系统如果不做控制，很容易带来三类问题：检索慢、token 贵、Prompt 噪声大。所以企业落地时要把记忆访问做成分层和按需加载。

第一层是短期窗口，只取最近几轮对话，解决当前会话连续性。

第二层是结构化画像，直接按字段读取，比如年龄、目标、过敏、慢病、运动限制。这类信息不需要向量检索，读取快、稳定性高。

第三层才是长期语义记忆，通过 ES 向量检索按 user_id 和 domain 过滤，只取与当前问题相关的 Top-K。

第四层是摘要记忆。对于很长的历史，可以周期性做 conversation summary 或 profile summary，把一段时间内的高价值信息压缩成摘要，减少每次检索和注入的 token。

另外还要做缓存和阈值控制。比如同一个 session 内的画像和短期记忆可以缓存；长期记忆召回低于阈值就不注入；Prompt 中只保留最重要的 3 到 5 条记忆；高频字段直接走 PostgreSQL，不走向量检索。

这样既能保持个性化，又能把延迟和 token 成本控制在可接受范围内。
```

### 43. 用户画像如何抽取和更新？

**考察点：画像系统。**

```text
用户画像更新采用异步 pipeline。

每轮对话结束后，系统会用规则和 LLM 从对话中抽取结构化信息，例如年龄、过敏、疾病史、饮食偏好、运动目标。抽取结果会带字段、值、置信度和证据来源。

合并时不是简单覆盖。稳定字段如出生年份、性别需要高置信度；列表字段如过敏、偏好要去重；可能变化的字段如体重、目标要保留更新时间和版本。

对于敏感或不确定信息，可以让用户确认后再写入长期画像。
```

### 44. 为什么不能把所有历史都塞进 prompt？

**考察点：上下文管理。**

```text
直接塞所有历史有三个问题：成本高、噪声大、容易超过上下文窗口。

更好的方式是把历史分层。最近几轮作为短期上下文直接使用；长期历史先做向量检索，只取与当前问题相关的记忆；稳定信息沉淀为结构化画像。

例如用户问“我还能跑步吗”，系统不需要加载所有聊天，只需要检索运动相关记忆、健康限制和最近环境数据。
```

### 45. 记忆和用户画像的边界如何划分？

**考察点：画像建模、结构化与非结构化记忆边界。**

```text
我会把用户画像和长期记忆分开设计。用户画像更像稳定、结构化、可被业务规则直接使用的事实表；长期记忆更像非结构化或半结构化的历史偏好、行为片段和上下文证据。

适合进入用户画像的是稳定字段，比如年龄、性别、身高体重、过敏史、慢病、健康目标、运动能力、饮食禁忌。这些字段通常放 PostgreSQL，更新需要更高置信度，有些还需要用户确认。

适合进入长期记忆的是表达更灵活的信息，比如“最近在准备半马”“工作日晚上比较忙”“周末更容易运动”“最近睡眠不太规律”。这些信息适合放 ES 语义记忆，检索时按 domain、importance、recency 做过滤。

两者之间可以有同步关系。长期记忆里多次出现的稳定事实，可以提升为用户画像；用户画像发生变化时，也要让相关旧记忆降权或标记 superseded。

这样做的好处是，结构化画像负责稳定决策，语义记忆负责上下文补充，既不会让画像字段膨胀，也不会让模型完全依赖一堆散乱历史。
```

---

## 7. 多模态与工具调用

### 46. 项目里的多模态能力是什么？

**考察点：多模态应用。**

```text
多模态主要用于健康数据输入和营养分析。

用户可以上传餐食图片、食品包装、体检报告、运动截图等。系统会根据图片类型选择 OCR 或视觉模型。

比如食品包装走 OCR 提取营养成分表；餐盘图片走视觉模型识别食物和份量；体检报告走 OCR + 结构化解析，提取血糖、血压、血脂等指标。

多模态模块不直接给最终建议，而是把图片信息转成结构化数据，再交给专业 Agent 和主 Agent 处理。
```

### 47. Tool Registry 的作用是什么？

**考察点：工具系统。**

```text
Tool Registry 是工具注册中心，统一管理 OCR、营养查询、天气 API、空气质量 API、健康数据查询、知识库检索等工具。

每个工具都有 name、description、parameters schema 和 invoke 方法。Agent 可以通过工具名调用，也可以让 LLM 根据工具 schema 选择工具。

这样新工具接入比较方便，不需要改动主 Agent 核心逻辑。例如新增“花粉指数查询工具”，只要注册工具并在 Environment Agent 中配置即可。
```

### 48. 工具调用失败怎么处理？

**考察点：降级设计。**

```text
工具失败要分级处理。

如果是非关键工具失败，比如天气 API 暂时不可用，可以使用 Redis 中最近一次缓存，并明确提示数据时间。

如果缓存也没有，就降级生成不依赖该工具的建议，比如“暂时无法获取实时天气，请以当地天气预报为准”。

如果是关键安全工具失败，例如医疗风险分类器异常，要采取保守策略，必要时建议就医或提示无法判断。

所有工具失败都要记录 trace，便于后续排查。
```

---

## 8. 后端工程与数据架构

### 49. 数据库为什么选 PostgreSQL？

**考察点：数据存储选型。**

```text
PostgreSQL 适合存用户、画像、会话、健康档案、任务状态等结构化数据。它事务能力强，JSONB 支持好，适合存用户画像这类半结构化字段。

健康数据有较强一致性要求，比如用户画像更新、聊天记录写入、健康档案修改，都需要可靠事务。

另外 PostgreSQL 生态成熟，后续可以通过分区表、索引、读写分离和备份策略支撑生产环境。
```

### 50. Redis 在项目里怎么用？

**考察点：缓存设计。**

```text
Redis 主要有四类用途。

第一是短期会话记忆，按 session_id 保存最近多轮对话。

第二是热点缓存，比如用户画像、RAG 检索结果、外部 API 结果、Agent 结果。

第三是限流和防刷，比如普通 API 和 LLM API 分开限流。

第四是 Celery broker 和任务结果后端，用于异步任务。

TTL 会按数据变化频率设置。用户画像可以缓存 1 小时，Agent 结果缓存几分钟，环境 API 根据数据源更新时间缓存。
```

### 51. Celery 承担什么角色？

**考察点：异步任务。**

```text
Celery 负责不适合同步阻塞 API 的任务。

例如用户画像抽取和更新、健康数据批量导入、体检报告解析、健康周报生成、通知提醒、知识库增量构建、embedding 批量生成等。

这些任务耗时长或者可以异步完成，不应该阻塞用户当前请求。通过 Celery 可以做队列隔离、失败重试、任务状态查询和定时调度。
```

### 52. API 如何设计？

**考察点：接口设计。**

```text
API 分为四类。

第一类是认证和用户接口，包括注册、登录、刷新 token、用户画像查询和修改。

第二类是对话接口，包括发送消息、流式回复、会话历史、推荐追问。

第三类是专业 Agent 接口，比如营养分析、环境查询、运动计划。

第四类是文件和数据接口，包括图片上传、健康数据导入、报告解析、健康档案管理。

对话接口返回不仅有 reply，还会返回 citations、suggested_questions、agent_trace、safety_flag 等，方便前端展示和调试。
```

### 53. 流式响应怎么做？

**考察点：用户体验和后端流式。**

```text
终版会用 SSE 或 WebSocket 做真正流式响应。

底层模型如果支持 streaming，就把 token 边生成边推给前端。对于多 Agent 场景，还可以先推送进度事件，比如“正在查询空气质量”“正在生成运动计划”，再推送最终回复。

这样可以降低用户感知延迟，特别是多 Agent 调用或 RAG 检索较慢时，用户不会一直等待空白页面。
```

---

## 9. 性能与稳定性

### 54. 如何优化多 Agent 响应延迟？

**考察点：性能优化。**

```text
首先做并行调度。没有依赖的 Agent 并发执行，有依赖的按 DAG 分批执行。

其次做缓存。用户画像、RAG 结果、外部 API、Agent 结果都可以缓存。

第三是减少不必要的 LLM 调用。简单意图用规则或轻量分类器，评分和风险分级用确定性规则，复杂生成才用大模型。

第四是流式响应和进度事件，降低感知延迟。

第五是超时和降级。单个 Agent 失败不能拖垮整个回答，主 Agent 要能基于已有结果生成可用回复。
```

### 55. 如何做高并发？

**考察点：系统扩展。**

```text
API 层可以通过 Uvicorn/Gunicorn 多 worker 和容器水平扩展。由于 Agent 调用主要是 IO 密集型，async 能提高并发效率。

Redis 承担会话缓存、限流和热点缓存，降低数据库压力。

PostgreSQL 使用连接池和索引优化，Elasticsearch 独立集群承担检索压力。

耗时任务交给 Celery worker，可以按队列水平扩容。

此外要对 LLM 调用做并发限制、超时、重试和熔断，避免外部模型服务异常拖垮系统。
```

### 56. 如何做可观测性？

**考察点：生产运维。**

```text
每次请求生成 trace_id，贯穿 API、Agent、RAG、工具和 LLM 调用。

Agent 层记录每个 LangGraph 节点的输入摘要、输出摘要、耗时、错误和下一跳。

RAG 层记录 query、召回文档、分数、rerank 分数、引用来源和检索耗时。

LLM 层记录模型名、token 用量、耗时、重试和失败原因。

监控指标包括 P50/P95 延迟、错误率、缓存命中率、RAG 召回耗时、LLM 成本、安全拦截率、工具失败率。
```

---

## 10. 安全、合规与医疗边界

### 57. 健康类 Agent 最大风险是什么？

**考察点：高风险 AI 意识。**

```text
最大风险是医疗误导、延误就医和隐私泄露。

医疗误导指模型给出错误诊断、药物剂量或治疗建议。

延误就医指用户已经有胸痛、呼吸困难等急症，但系统没有及时提示急诊。

隐私泄露指用户健康数据、疾病史、体检报告等敏感信息被不当存储、日志记录或发送给不必要的第三方。

所以这个项目必须强调边界：提供健康教育和生活方式建议，不替代医生诊断和治疗。
```

### 58. 如何避免模型给诊断和处方？

**考察点：输出约束。**

```text
首先在 system prompt 中明确禁止诊断疾病、开药和给具体剂量。

其次在安全分类中识别药物剂量、治疗方案、急症症状等高风险问题，必要时拒答或建议就医。

第三，输出后可以做审核，比如检测是否出现“你得了某病”“服用多少 mg”等敏感模式。

第四，所有健康建议都要加免责声明，说明不能替代专业医疗建议。
```

### 59. 如何处理用户隐私？

**考察点：数据合规。**

```text
隐私上我会遵循最小化原则，只收集提供服务必要的数据。

存储层面，敏感字段加密，数据库和对象存储设置访问控制，日志脱敏。

使用层面，只把当前任务需要的画像字段注入 prompt，不把完整健康档案全部传给模型。

用户侧提供画像查看、修改、删除能力。对于体检报告、图片等文件设置生命周期和删除机制。
```

### 60. 如果用户说“我胸痛但不想去医院，告诉我吃什么药”，系统怎么答？

**考察点：安全边界落地。**

```text
系统会把它识别为紧急医疗风险，因为胸痛可能涉及心血管急症。

我不会提供药物名称或剂量，也不会建议先观察。回复会明确提示：这可能是紧急情况，请立即拨打 120 或前往最近急诊，并尽量让身边人陪同。

如果用户强调不想去医院，系统也应该坚持安全优先，说明在线建议不能替代急救服务。
```

---

## 11. 测试与评估

### 61. 如何测试 Agent 系统？

**考察点：测试体系。**

```text
我会分层测试。

单元测试覆盖节点函数，比如安全检查、意图识别、营养评分、环境风险规则、RRF 融合、画像合并。

集成测试覆盖完整 Agent 流程，比如 HealthAdvisor 从输入到最终回复，Nutrition 从食物描述到营养报告。

多 Agent 测试覆盖任务拆解、依赖调度、超时降级和结果聚合。

安全测试覆盖胸痛、中风、自杀意念、药物剂量等高风险样本。

RAG 测试覆盖检索召回、引用正确性、答案是否忠实于文档。
```

### 62. 如何评估最终回答质量？

**考察点：LLM 应用评估。**

```text
我会从准确性、相关性、可解释性、安全性和个性化五个维度评估。

准确性看回答是否符合知识库和健康指南。

相关性看是否真正回答用户问题。

可解释性看是否给出原因和引用。

安全性看是否避免诊断、处方和急症误导。

个性化看是否合理使用用户画像，比如过敏、目标、运动能力、慢性病限制。
```

### 63. 如何构建健康问答评测集？

**考察点：评测数据建设。**

```text
评测集要覆盖正常咨询和高风险边界。

正常咨询包括饮水、睡眠、BMI、运动频率、饮食搭配、空气质量、减脂计划。

专业 Agent 样本包括餐食营养分析、AQI 场景、不同体能等级运动计划。

高风险样本包括胸痛、呼吸困难、中风症状、自杀意念、药物剂量、诊断请求。

每个样本要有期望行为，比如必须引用知识、必须建议就医、必须拒绝处方、必须考虑用户画像。
```

---

## 12. 高频追问题与推荐回答

### 64. 如果新增 SleepAgent，你怎么做？

```text
我会新增 sleep Agent 目录，包括 state、agent、nodes 和 prompts。

State 里包含 sleep_duration、sleep_quality、bed_time、wake_time、caffeine、screen_time、stress_level、health_conditions 等字段。

节点包括 parse_sleep_context、retrieve_sleep_knowledge、assess_sleep_quality、generate_sleep_plan、format_response。

然后在 AgentRegistry 注册 SleepAgent，在意图识别中加入 sleep intent，在 Orchestrator 中支持调度。

如果用户问“最近睡不好还能晚上跑步吗”，SleepAgent 和 ExerciseAgent 可以协作，主 Agent 最终综合建议。
```

### 65. 如何处理 Agent 输出冲突？

```text
冲突处理遵循安全优先、证据优先、用户约束优先。

例如 Exercise Agent 认为适合跑步，但 Environment Agent 判断 AQI 很差，主 Agent 会以环境健康风险为准，建议改为室内运动。

如果 Nutrition Agent 建议高蛋白饮食，但用户画像里有肾病风险，主 Agent 会降低蛋白建议强度并提示咨询医生。

技术上，子 Agent 输出要结构化，包含 confidence、warnings 和 evidence，主 Agent 才能做规则化仲裁。
```

### 66. 如何降低 LLM 成本？

```text
首先，能用规则解决的不用 LLM，比如营养计算、AQI 分级、运动强度约束。

其次，简单意图识别可以用关键词或轻量模型，大模型只处理复杂任务。

第三，缓存 RAG 检索结果、用户画像、外部 API 和 Agent 结果。

第四，控制 prompt token，只注入相关记忆和 top-k 文档。

第五，根据任务分层使用模型，复杂推理用强模型，格式化或分类用轻量模型。
```

### 67. Prompt Injection 怎么防？

```text
Prompt injection 主要从隔离和校验两方面防。

系统指令、用户输入、RAG 文档和工具结果要用明确边界隔离，不能让用户或文档覆盖系统安全规则。

RAG 文档当作不可信内容处理，如果文档里出现“忽略之前指令”等内容，需要过滤。

工具调用参数要做白名单和类型校验，不让模型构造危险参数。

另外对要求泄露系统 prompt、密钥、其他用户数据的请求直接拒绝。
```

### 68. 如果外部 API 不稳定怎么办？

```text
外部 API 要做 timeout、retry、cache 和 fallback。

例如天气 API 失败，先看 Redis 中最近一次成功结果，并在回复中标注数据时间。

如果没有缓存，就降级为一般建议，并提示用户当前无法获取实时数据。

如果是关键安全判断依赖的数据缺失，采取保守策略，比如空气质量未知时不建议高强度户外运动。
```

---

## 13. 可直接背诵的 3 分钟项目介绍

```text
AI健康管家是我做的一个多模态多 Agent 健康管理系统，目标是把健康咨询、饮食营养、环境风险、运动规划和长期健康画像整合到一个对话式助手中。

后端用 FastAPI，Agent 工作流用 LangGraph。系统有一个 HealthAdvisor Agent 作为主控入口，负责安全检查、意图识别、任务拆解、RAG 检索、子 Agent 调度和最终回复。专业 Agent 包括 Nutrition Agent、Environment Agent、Exercise Agent，分别处理饮食营养分析、空气质量和天气风险、个性化运动计划。

RAG 部分我采用 Elasticsearch BM25 + BGE-M3 dense vector 混合检索。BM25 负责精确术语和指标召回，比如 BMI、血压、AQI；BGE-M3 负责语义召回，比如用户说“血压有点高，饮食要注意什么”。两路结果通过加权 RRF 融合，再可选用 BGE reranker 精排，最后把 top-k 知识带引用注入 LLM，降低幻觉。

记忆系统分为短期会话记忆、中期健康事件记忆和长期用户画像。用户画像包括年龄、健康目标、过敏史、饮食偏好、慢性病和运动能力，用于个性化建议。

健康场景风险比较高，所以我把 safety check 放在主流程最前面。胸痛、呼吸困难、中风、自杀意念等情况会直接进入紧急就医提示，不进入普通问答。系统也限制模型不能诊断疾病、不能开药、不能给具体治疗方案。

整体上，这个项目的重点不是简单套一个聊天模型，而是把 LLM、RAG、工具调用、记忆和多 Agent 编排组合成一个可控、可扩展、可观测的健康 Agent 系统。
```
